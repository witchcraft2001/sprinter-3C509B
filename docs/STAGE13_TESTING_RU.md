# Ручная приёмка Stage 13 в MAME

Используется отдельный образ Stage 13 со статическим адресом `192.168.7.20` и
FTP/host responder на `192.168.7.44` (имя `ftp.stage13.test`). До запуска MAME
подготовьте образ и запустите responder в том же host-only Ethernet-сегменте.
Сохраните его текстовый лог и pcap на протяжении всей проверки.

Для реального Sprinter raw-Ethernet responder на `feth1` не используется: это
виртуальный интерфейс MAME. На Mac запустите TCP-режим на адресе физического
интерфейса (например, `192.168.1.36`), подключённого к той же сети:

```text
STAGE13_HTTP_BIND=192.168.1.36 tools/stage13-mame.sh responder-http
```

Либо передайте адрес явно: `tools/stage13-mame.sh responder-http
192.168.1.36 8080`. По умолчанию используется непривилегированный TCP-порт
8080; в DSS в этом случае укажите один и тот же URL
`http://192.168.1.36:8080/` для `DLDIRECT` и `DLSPEED`. Порт 80 можно выбрать
через `STAGE13_HTTP_PORT=80` (на macOS для него может потребоваться `sudo`).
Запись pcap для физического теста
делается отдельно (`tcpdump`/Wireshark) на том же Ethernet-интерфейсе.

В DSS выполните по порядку (responder — профиль `clean`, см. ниже про
единственное исключение):

```text
FTP /?
FTP ftp.stage13.test SMALL.BIN -y
FTP ftp.stage13.test LARGE.BIN -y -d
FTP ftp.stage13.test -l
FTP ftp.stage13.test -n
FTP ftp.stage13.test SMALL.BIN -o COPY.BIN -y
FTP ftp.stage13.test PUT COPY.BIN -o UPLOAD.BIN
FTP ftp.stage13.test LARGE.BIN -r
DLSPEED /?
DLDIRECT /?
DLDIRECT http://ftp.stage13.test/
DLSPEED http://ftp.stage13.test/
DLSPEED http://ftp.stage13.test/
DLDIRECT http://ftp.stage13.test/
```

**Отдельно, после перезапуска responder'а с `--profile refuse-pass`**
(`STAGE13_PROFILE=refuse-pass tools/stage13-mame.sh responder`) — MAME-сессию
и IP-конфигурацию перезапускать не нужно, `FTP` каждый раз заново резолвит
ARP:

```text
FTP ftp.stage13.test -u baduser -p badpass SMALL.BIN -y
```

Профиль `clean` принимает любой пароль (это макет сервера, а не настоящая
проверка учётных данных), поэтому с ним эта команда закономерно вернёт
`RESULT OK` — это не баг FTP.EXE, а свойство тестового стенда. Чтобы увидеть
`530`, нужен именно `refuse-pass`.

Ожидается баннер `3C509B FTP v...`, затем `Host ИМЯ -> IP`, ответы сервера
построчно (`220 ...`, `331 ...` + `230 ...`, `200 ...`, `213 ...`, `227 ...`),
строка `PASV IP:ПОРТ` с разобранным адресом канала данных, `Opening data...`,
`150 ...`, прогресс `X KB / Y KB` (или точки с `-d`), `226 Transfer
complete.`, `Done. N bytes recv.`/`... sent.`, строка `  N bytes in S sec`,
`221 Goodbye.` и `RESULT OK`. Формулировки короче, чем у сиблинга, — так они
влезли в потолок образа (см. `docs/FTP.md`, "Known limitations"); точные
транскрипты зафиксированы в `tools/test-fixtures/stage13-ftp-golden.json`.
`226` должен появляться и на мелких файлах: он приходит по control-каналу,
пока ещё качается канал данных, и не теряется. Листинг (`-l`
и `-n`) печатает содержимое каталога прямо на консоль без записи файла; `-n` в
этой сборке выводит то же самое, что и `-l` (см. `docs/FTP.md`, раздел "Known
limitations" — NLST-fallback не влезает в потолок образа 16256 байт).

Перед тестом resume подготовьте `LARGE.BIN` с уже частично скачанным
префиксом (например, тем же способом, что и `RANGE.BIN` в Stage 12 — см.
`docs/STAGE12_TESTING_RU.md`). После `-r` в логе responder'а должна быть видна
команда `CMD 'REST <offset>'`, а итоговый файл — побайтно совпадать с полным
fixture, **без задвоения уже скачанной части**: responder честно отдаёт
только хвост файла после offset (это было исправлено в тестовой
инфраструктуре — раньше REST принимался, но игнорировался, и RETR всегда
пересылал файл с нуля, задваивая содержимое при resume).

Повторите `FTP ftp.stage13.test LARGE.BIN -r`, когда локальный `LARGE.BIN`
уже полный. После `213 3000` клиент не должен открывать data-соединение и
посылать `REST 3000`/`RETR`: ожидаются `Done. 0 bytes recv.`, `221`,
`RESULT OK`, а файл остаётся неизменным. Responder также корректно завершает
FIN-only пустую передачу, если `SIZE` недоступен и REST всё же совпал с EOF.

Во время большой загрузки один раз нажмите Esc или Ctrl+C. Ожидаются
сохранённый partial-файл, `Aborted (Esc/^C).`, `RESULT FAIL` и возврат в DSS
без зависания. Повторите этот файл с `-r` и убедитесь, что содержимое
восстановлено полностью.

Обе утилиты печатают `Waiting for RTC edge; transfer is silent until done...`,
затем без промежуточного вывода `Received: 4194304 bytes`, строку `N bytes in
S sec, R KB/s` и `RESULT OK`. `DLDIRECT` — оптимизированный прямой путь;
`DLSPEED` загружает расположенный рядом `UNET509B.DLL` и измеряет публичный
UNET/libman путь. `stage13_responder.py` отвечает на обычный HTTP GET любой
4-МиБ фикстурой `HTTP_LARGE` с `Content-Length` и keep-alive (не путать с
FTP-шным 3000-байтным `LARGE.BIN`). Поэтому клиент обязан остановиться точно
на объявленной длине и закрыться штатным FIN без RST, не ожидая FIN сервера.

Порядок запуска уже образует первый блок двухточечного сравнения:
`DLDIRECT`, `DLSPEED`, `DLSPEED`, `DLDIRECT`. Повторите его отдельно на
обычном release-образе и на заранее собранном непакуемом
`build/perf-fast/sprinter-3c509b-fast.img`, пока для каждого образа не будет
минимум пяти успешных результатов каждой утилиты. На одном MAME-стенде
обязательны медианы: для release `DLDIRECT >= 150 KB/s` и
`DLSPEED / DLDIRECT >= 40%`; для fast `DLDIRECT >= 180 KB/s` и
`DLSPEED >= 76 KB/s`. Для каждой утилиты обязателен успешный повторный запуск
без зависания. Подробности и формат записи — в `docs/DLSPEED.md`.

Fast-образ уже содержит `STAGE13.CFG` под именем `NET.CFG`; запустите его в
том же MAME и с тем же responder/URL, подставив этот файл вместо release IMG.
Не смешивайте результаты двух образов в одну медиану.

В pcap release-образа проверьте полные TCP/IPv4 checksum. Окно DLL не должно
превышать 2680 байт: первый прямо принятый сегмент расширяет исходные 536
байт, следующие подтверждаются обычно парами (ACK на 1072 байта), а последний
ACK перед возвратом из `RECV` снова обещает только долговечные 536 байт
pending. DLDIRECT, в отличие от DLL, анонсирует MSS 1460: в его SYN должна
стоять опция `MSS=1460`, а сегменты тела — приходить по 1460 байт. Если в
pcap видны сегменты по 536 байт, значит MSS проигнорировал сервер, а не
клиент его не запросил; такой прогон недействителен для замера скорости.
Обычные established ACK DLDIRECT, включая ACK на границе `RECV`, должны
сохранять выбранное по Window 3 Free Receive Bytes окно: 11680 байт
(8 MSS) у текущей модели MAME с 16-КиБ RX FIFO либо 4380 байт (3 MSS) у
карты, где свободно меньше 12128 байт. Меньшее окно допустимо только при
настоящем заполнении pending. `win=0` на каждом MSS или один публичный
`RECV` на каждый сегмент — отказ оптимизации. На чистом потоке DLDIRECT
cumulative ACK должен обычно продвигаться на 2 сегмента (2920 байт): это
пополняет скользящее окно до его исчерпания.
Ошибка, нестандартный пакет, FIN или неполный drain подтверждаются раньше. На
fast-образе исходящие checksum остаются обязательными;
единственная допустимая разница — отсутствие проверки TCP checksum у
штатного established in-order data-сегмента.

Ту же двухобразную последовательность повторите на реальном Sprinter после
read-only определения карты, слота и base. Запишите MAC, скорости, консольные
логи и pcap отдельно от MAME; MAME не закрывает аппаратную приёмку.

Отдельно проверьте скорость отдачи. Загрузите на сервер файл заведомо больше
двух сегментов — подойдёт `LARGE.BIN`, лучше несколько десятков килобайт:

```text
FTP ftp.stage13.test LARGE.BIN -y
FTP ftp.stage13.test PUT LARGE.BIN -o BIG_UP.BIN
```

Ожидается `RESULT OK`, побайтное совпадение выгруженного файла с исходным и
строка `N bytes in S sec, R KB/s`, сопоставимая по порядку со скоростью
скачивания того же файла, а не втрое ниже. В pcap на канале данных должны
идти пары сегментов по 536 байт подряд с последовательными номерами и одно
накопительное подтверждение на пару: два `len=536` от клиента, затем `ACK` на
1072 байта вперёд. Один сегмент на подтверждение означает, что окно отправки
не сформировалось — проверьте окно, которое анонсирует сервер (нужно не
меньше 1280 байт), и приложите этот pcap к отчёту.

Соберите доказательства одного непрерывного gate: скриншоты `/?`, листинга,
ошибки логина, resume, PUT и Esc/Ctrl+C; responder log; pcap (проверьте SYN на
управляющий порт 21, ответ `227` и SYN на объявленный порт данных, до 5
сегментов в очереди; для FTP — отсутствие `win=0`); checksum образа, `FTP.EXE`,
`DLSPEED.EXE`, `DLDIRECT.EXE`, fixtures и файлов, извлечённых из FAT12. Для
DLL-трассы допустимо только начальное закрытие окна до входа в `RECV`, но не
`win=0` на каждом MSS. Заполните
`docs/evidence/STAGE13_TEST_TEMPLATE.md`. До наличия этих файлов MAME-флажок в
`specs.md` остаётся открытым; проверка реальной карты выполняется отдельно.
