# Ручная приёмка Stage 13 в MAME

Используется отдельный образ Stage 13 со статическим адресом `192.168.7.20` и
FTP/host responder на `192.168.7.44` (имя `ftp.stage13.test`). До запуска MAME
подготовьте образ и запустите responder в том же host-only Ethernet-сегменте.
Сохраните его текстовый лог и pcap на протяжении всей проверки.

В DSS выполните по порядку (responder — профиль `clean`, см. ниже про
единственное исключение):

```text
FTP /?
FTP ftp.stage13.test SMALL.BIN -y
FTP ftp.stage13.test LARGE.BIN -y -d
FTP ftp.stage13.test -l
FTP ftp.stage13.test -n
FTP ftp.stage13.test SMALL.BIN -o COPY.BIN -y
FTP ftp.stage13.test PUT COPY.BIN -o UPLOAD.BIN
FTP ftp.stage13.test LARGE.BIN -r
DLSPEED /?
DLSPEED http://ftp.stage13.test/
```

**Отдельно, после перезапуска responder'а с `--profile refuse-pass`**
(`STAGE13_PROFILE=refuse-pass tools/stage13-mame.sh responder`) — MAME-сессию
и IP-конфигурацию перезапускать не нужно, `FTP` каждый раз заново резолвит
ARP:

```text
FTP ftp.stage13.test -u baduser -p badpass SMALL.BIN -y
```

Профиль `clean` принимает любой пароль (это макет сервера, а не настоящая
проверка учётных данных), поэтому с ним эта команда закономерно вернёт
`RESULT OK` — это не баг FTP.EXE, а свойство тестового стенда. Чтобы увидеть
`530`, нужен именно `refuse-pass`.

Ожидается баннер `3C509B FTP v...`, затем `Host ИМЯ -> IP`, ответы сервера
построчно (`220 ...`, `331 ...` + `230 ...`, `200 ...`, `213 ...`, `227 ...`),
строка `PASV IP:ПОРТ` с разобранным адресом канала данных, `Opening data...`,
`150 ...`, прогресс `X KB / Y KB` (или точки с `-d`), `226 Transfer
complete.`, `Done. N bytes recv.`/`... sent.`, строка `  N bytes in S sec`,
`221 Goodbye.` и `RESULT OK`. Формулировки короче, чем у сиблинга, — так они
влезли в потолок образа (см. `docs/FTP.md`, "Known limitations"); точные
транскрипты зафиксированы в `tools/test-fixtures/stage13-ftp-golden.json`.
`226` должен появляться и на мелких файлах: он приходит по control-каналу,
пока ещё качается канал данных, и не теряется. Листинг (`-l`
и `-n`) печатает содержимое каталога прямо на консоль без записи файла; `-n` в
этой сборке выводит то же самое, что и `-l` (см. `docs/FTP.md`, раздел "Known
limitations" — NLST-fallback не влезает в потолок образа 16256 байт).

Перед тестом resume подготовьте `LARGE.BIN` с уже частично скачанным
префиксом (например, тем же способом, что и `RANGE.BIN` в Stage 12 — см.
`docs/STAGE12_TESTING_RU.md`). После `-r` в логе responder'а должна быть видна
команда `CMD 'REST <offset>'`, а итоговый файл — побайтно совпадать с полным
fixture, **без задвоения уже скачанной части**: responder честно отдаёт
только хвост файла после offset (это было исправлено в тестовой
инфраструктуре — раньше REST принимался, но игнорировался, и RETR всегда
пересылал файл с нуля, задваивая содержимое при resume).

Во время большой загрузки один раз нажмите Esc или Ctrl+C. Ожидаются
сохранённый partial-файл, `Aborted (Esc/^C).`, `RESULT FAIL` и возврат в DSS
без зависания. Повторите этот файл с `-r` и убедитесь, что содержимое
восстановлено полностью.

`DLSPEED` печатает `Waiting for RTC edge; transfer is silent until done...`,
затем (без промежуточного вывода) `Received: N bytes`, строку `N bytes in S
sec, R KB/s` и `RESULT OK`. `stage13_responder.py` теперь сам отвечает и на
обычный HTTP GET (любой путь — отдаёт отдельную 512-КиБ фикстуру `HTTP_LARGE`,
не путать с FTP-шным 3000-байтным `LARGE.BIN`), так что DLSPEED работает
против того же responder'а, без переключения на образ/responder Stage 12.
512 КиБ выбраны намеренно: RTC тикает целыми секундами, и файл, укладывающийся
в одну секунду (как 70-килобайтный `LARGE.BIN` из Stage 12 на виртуальном
линке — 68 КБ/с за 1 сек), либо получает от самого DLSPEED отказ "sample too
short", либо даёт грубую, недостоверную оценку KB/s (см. `docs/DLSPEED.md`,
там же рекомендованы "at least 512 KiB"). Совместно с приёмкой этого этапа
сравните измеренную скорость с прежними ~3 КБ/с (до исправления TCP receive
window, см. `specs.md`) — ожидается
кратно более высокое и равномерное значение.

Отдельно проверьте скорость отдачи. Загрузите на сервер файл заведомо больше
двух сегментов — подойдёт `LARGE.BIN`, лучше несколько десятков килобайт:

```text
FTP ftp.stage13.test LARGE.BIN -y
FTP ftp.stage13.test PUT LARGE.BIN -o BIG_UP.BIN
```

Ожидается `RESULT OK`, побайтное совпадение выгруженного файла с исходным и
строка `N bytes in S sec, R KB/s`, сопоставимая по порядку со скоростью
скачивания того же файла, а не втрое ниже. В pcap на канале данных должны
идти пары сегментов по 536 байт подряд с последовательными номерами и одно
накопительное подтверждение на пару: два `len=536` от клиента, затем `ACK` на
1072 байта вперёд. Один сегмент на подтверждение означает, что окно отправки
не сформировалось — проверьте окно, которое анонсирует сервер (нужно не
меньше 1280 байт), и приложите этот pcap к отчёту.

Соберите доказательства одного непрерывного gate: скриншоты `/?`, листинга,
ошибки логина, resume, PUT и Esc/Ctrl+C; responder log; pcap (проверьте SYN на
управляющий порт 21, ответ `227` и SYN на объявленный порт данных, до 5
сегментов в очереди и отсутствие `win=0`); checksum образа, `FTP.EXE`,
`DLSPEED.EXE`, fixtures и файлов, извлечённых из FAT12. Заполните
`docs/evidence/STAGE13_TEST_TEMPLATE.md`. До наличия этих файлов MAME-флажок в
`specs.md` остаётся открытым; проверка реальной карты выполняется отдельно.
