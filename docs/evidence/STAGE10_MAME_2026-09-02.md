# Stage 10 MAME acceptance — 2026-09-02

- Result: PARTIAL — manual service checks passed; current mutable image still
  contains an incomplete GET output file, so the MAME gate remains open.
- Image: `build/stage10-mame/stage10-dhcp.img`
- Pcap: `evidence/stage10/stage10-faults.pcap`
- Screenshots: user-provided MAME captures from 2026-09-02 (DHCP, renewal,
  DNS, NTP, TFTP cancel/PUT, and release/cleanup).

## DSS results extracted from the manual session

- DHCP acquire: `RESULT OK`; lease `192.168.7.20`, server `192.168.7.1`,
  lease `86400` seconds.
- DHCP renewal: `DHCP RENEW ACK IP=192.168.7.20`, `RESULT OK`.
- `NSLOOKUP echo.stage10.test`: `192.168.7.44`, `RESULT OK`.
- Public `NSLOOKUP example.com 1.1.1.1`: `104.20.23.154`, `RESULT OK`.
- NTP with `TZ=+5:45`: `2025-01-01 17:44:59 UTC+05:45`, `RESULT OK`.
- Public `NTP pool.ntp.org`: `RESULT OK`.
- TFTP cancellation: `RESULT FAIL code=23` (expected cancellation status).
- TFTP PUT: `RESULT OK`.
- DHCP release: `DHCP RELEASED`, `RESULT OK`; subsequent `NETCFG` has no
  dynamic IP, mask, gateway, DNS, DHCP server, or lease. Repeated `IFUP -d`
  returned `RESULT OK` without a lease.

## Responder/pcap evidence

`tools/stage10-mame.sh pcap-check evidence/stage10/stage10-faults.pcap`:

```
PCAP OK frames=4603 dhcp=14 release=1 dns=39 ntp=17 icmp=0 echo=30
tftp=987 checksums=valid no-fcs-record-lengths=exact
```

The checker reconstructs both TFTP fixtures from the capture and validates
every DATA block. Thus the capture contains complete byte-exact GET and PUT
transfers, including the injected loss/retry/duplicate traffic.

The responder's terminal log was not redirected to a file, so its individual
`READY`/`FRAME`/`DROP`/`RETRY` text lines cannot be recovered retrospectively;
the pcap is the durable protocol evidence.

## Current image-file check

`tools/stage10-mame.sh verify` currently fails only for GET:

```
cmp: EOF ... after byte 71400, in line 280
```

The fixture is 262267 bytes; the current `S9GET.BIN` in the mutable FAT image
is 71400 bytes (`50 * 1428`). This is the output of the later interrupted GET
used to test cancellation, not the complete transfer reconstructed from the
pcap. PUT is byte-exact:

```
c38b132f0e7a7b4542e25a231d06ab244b8de019e49234a1f4f2525ac28254ce
```

The remaining manual action for a fully closed MAME gate is one uninterrupted
GET to completion, followed by `tools/stage10-mame.sh verify`. Hardware
acceptance remains open as required by `specs.md`.
