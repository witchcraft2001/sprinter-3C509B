# Stage 8 MAME evidence — 2026-09-01

Ручная приёмка выполнена в MAME с `feth0`/`feth1`, static-конфигурация
`192.168.7.20/24`, шлюз `192.168.7.1`, локальный peer `192.168.7.44` и
внешний peer `203.0.113.10`. После перезапуска MAME выполнены `NETCFG -i` и
`IFUP`.

## Результаты

- direct peer, gateway и routed external — `RESULT OK`;
- payload `0/1/32/1472` и TTL `1/255` — `RESULT OK`;
- `PINGALT` для direct и external — `RESULT OK`;
- `noise` — корректный reply после unrelated/bad-checksum кадров;
- `unreachable` — `RESULT FAIL code=24`, DSS exit 6;
- `drop` — `[E2] TIMEOUT`, `RESULT FAIL code=14`, без зависания;
- `PING -t` — отмена Esc и Ctrl+C, `RESULT FAIL code=23`, DSS exit 7;
- endurance MAME — `PING -n 100 -l 0 192.168.7.44`, 100/100 replies,
  `RESULT OK`;
- автоматический actual-EXE endurance отдельно проверяет 1000/1000 запросов.

## Captures

Все PCAP classic без FCS, exact record lengths:

| Файл | Кадры | SHA-256 |
|---|---:|---|
| `evidence/stage8/echo.pcap` | 208 | `65a35397219b416fa4872c85a4a33049a2f5521a2ecc1ceb4ade0194bb0c8e48` |
| `evidence/stage8/noise.pcap` | 8 | `69d9865ae9980ff9f99c665495abe446c39c94fae7fb70104721b6c50d50687e` |
| `evidence/stage8/unreachable.pcap` | 4 | `3d9955f5bf5a98f5312dbec2f43dde98bf92e3287ec4709ae989ae02029a3cf2` |
| `evidence/stage8/drop.pcap` | 3 | `c5cd6080b8bd55f99528b4f40034b77b9e6a8abd692862332a9e68beb03da2e4` |

`pcap-check` пройден для `echo`, `drop` и `unreachable`; `noise` содержит
намеренно повреждённые кадры и проверяется по сценарию, а не как полностью
валидный capture.

Аппаратная проверка на реальном Sprinter/3C509B-TPO не выполнялась.
