# Stage 9 MAME acceptance — 2026-09-01

- Result: **PASS (MAME gate)**
- Environment: Sprinter MAME, static `192.168.7.20/24`, responder on `feth1`, MAME on `feth0`
- Responder: `sudo tools/stage9-mame.sh responder faults`
- MAME: `tools/stage9-mame.sh mame`
- Pcap: `evidence/stage9/stage9-faults.pcap`
- Screenshot: user-provided `2026-09-01_22-13-02 (2).png`

## DSS results

- `NETCFG -i`: `RESULT OK`
- `IFUP`: `RESULT OK`, static IP `192.168.7.20`
- `UDPTEST -n 10 -l 1472 192.168.7.44 7777`: 10/10 byte-exact replies, `RESULT OK`
- TFTP GET `S9GET.BIN`: `RESULT OK`
- TFTP PUT `S9PUT.BIN`: `RESULT OK`
- Existing-file GET refusal: `RESULT FAIL code=23` (expected cancellation, exit 7)
- GET with `-y`: `RESULT OK`

The initial bare `UDPTEST` invocation printed usage and `code=21`; this is the
expected CLI argument error and is not part of the successful UDP run.

## Responder and pcap evidence

The responder log contains `READY`, ARP, UDP echo frames, request loss and
retry, DATA/ACK loss and retry, duplicate/reordered DATA, and unknown-TID
traffic. The final pcap check reported:

```text
PCAP OK frames=1318 udp=1308 tftp=1147 echo=20 get-data=752 put-data=4 byte-exact no-fcs-record-lengths=exact
```

`tools/stage9-mame.sh verify` reported `VERIFY OK GET and PUT fixtures are
byte-exact`.

## Open item

Real Sprinter/3C509B hardware acceptance remains open.
