# Сетевой кит Sprinter для 3Com 3C509B-TPO

## Статус документа

- Версия ТЗ: 1.0
- Дата: 2026-09-03
- Целевая платформа: Sprinter DSS
- Сетевая карта: 3Com EtherLink III 3C509B-TPO
- Режим шины: ISA8
- Состояние проекта: локальная кодовая часть этапов 0–11 реализована; MAME-матрица
  этапа 4 пройдена на `CYCLES21`; Stage 5/6 имеет частичные MAME pcap, но полная
  матрица остаётся открытой; автоматическая и ручная MAME-приёмка Stage 7
  пройдены; автоматическая и ручная MAME-приёмка Stage 8 пройдены;
  автоматическая и ручная MAME-приёмка Stage 9 пройдены; автоматическая Stage
  10-приёмка пройдена, а её ручной MAME gate ещё открыт; автоматическая и
  ручная Stage 11-приёмка пройдены; локальная кодовая часть Stage 12 реализована,
  а её MAME и проверки на реальном Sprinter ещё открыты

Этот документ одновременно является техническим заданием, дорожной картой и
журналом приёмки. Все этапы выполняются последовательно. Этап считается закрытым
только после установки всех обязательных флажков и добавления ссылки на лог,
дамп, pcap или иной воспроизводимый результат проверки.

Переход к следующему этапу разрешён после успешной автоматической и ручной
MAME-приёмки текущего этапа. Отсутствие доступного реального Sprinter этот
переход не блокирует: аппаратные флажки остаются открытыми до финальной
приёмки проекта на реальном Sprinter/3C509B, и до неё этап не считается
полностью закрытым. Host harness не заменяет MAME gate.

## Сводная таблица прогресса

| № | Этап | Код | MAME | Реальная карта | Документация | Закрыт |
|---:|---|:---:|:---:|:---:|:---:|:---:|
| 0 | Основа проекта и сборка | [x] | [x] | [ ] | [x] | [ ] |
| 1 | Аудит документации и референсов | N/A | [ ] | [ ] | [x] | [ ] |
| 2 | Исправление и проверка модели MAME | [x] | [ ] | N/A | [x] | [ ] |
| 3 | ID-последовательность и EEPROM | [x] | [ ] | [ ] | [x] | [ ] |
| 4 | Регистровый слой и инициализация | [x] | [x] | [ ] | [x] | [ ] |
| 5 | FIFO и внутренний loopback | [x] | [ ] | [ ] | [x] | [ ] |
| 6 | Физические TX и RX | [x] | [ ] | [ ] | [x] | [ ] |
| 7 | NETDRV, конфигурация, ARP и DHCP acquire | [x] | [x] | [ ] | [x] | [ ] |
| 8 | IPv4, ICMP и PING | [x] | [x] | [ ] | [x] | [ ] |
| 9 | UDP и TFTP | [x] | [x] | [ ] | [x] | [ ] |
| 10 | DHCP, DNS и NTP | [x] | [ ] | [ ] | [x] | [ ] |
| 11 | TCP | [x] | [x] | [ ] | [x] | [ ] |
| 12 | WGET | [x] | [ ] | [ ] | [x] | [ ] |
| 13 | FTP + DLSPEED | [x] | [ ] | [ ] | [x] | [ ] |
| 14 | UNET DLL и bindings | [ ] | [ ] | [ ] | [ ] | [ ] |
| 15 | TELNET и передачи файлов | [ ] | [ ] | [ ] | [ ] | [ ] |
| 16 | Стабильность, документация и релиз | [ ] | [ ] | [ ] | [ ] | [ ] |

## 1. Цель и границы проекта

### 1.1. Цель

Разработать автономный сетевой комплект для Sprinter DSS, работающий с
3C509B-TPO через восьмибитный ISA-интерфейс и предоставляющий тот же практически
полезный набор сетевых возможностей, что и соседние комплекты RTL8019AS и ESP:

- настройка карты и сети;
- статическая IPv4-конфигурация и DHCP;
- ARP, ICMP, UDP, DNS, NTP и TCP;
- PING, NSLOOKUP, TFTP, WGET, FTP и TELNET;
- совместимая UNET DLL;
- C- и Pascal-bindings;
- диагностические и нагрузочные программы;
- готовый IMG для MAME и ZIP для распространения.

### 1.2. Входит в версию 1

- Одна карта 3C509B-TPO.
- 10BASE-T через RJ-45.
- Half duplex по умолчанию.
- Ethernet II, ARP и IPv4.
- Работа карты исключительно опросом состояния.
- Чтение EEPROM и временная runtime-настройка I/O-базы.
- Тестирование в MAME и на реальном Sprinter.

### 1.3. Не входит в версию 1

- Аппаратные прерывания карты.
- Подключение или эмуляция IRQ-линий Sprinter.
- Запись EEPROM и изменение постоянной конфигурации карты.
- Несколько одновременно работающих сетевых адаптеров.
- BNC, AUI и автоматическое переключение трансиверов.
- Full duplex как пользовательская настройка.
- IPv6, TLS, HTTPS, gzip и HTTP chunked encoding.
- TCP LISTEN и серверные приложения.
- Plug and Play ISA configuration manager.

### 1.4. Паритет с соседними комплектами

Перед релизом эта таблица должна быть полностью закрыта. Она отделяет полезные
пользовательские программы от аппаратно-зависимых или тестовых эквивалентов.

| RTL/ESP-компонент | Эквивалент 3C509B | Куда входит | Решение |
|---|---|---|---|
| `NICINFO` | `EL3INFO` | IMG + ZIP | Основная безопасная диагностика карты |
| `ISAPROBE` | `ISAPROBE` | Только IMG | Явный безопасный ISA8 probe, без сканирования по умолчанию |
| `NICEEP` | `EL3EEP` | Только IMG | Полный read-only дамп EEPROM |
| `NICMODE` | Нет | Не входит | Требует изменения постоянной конфигурации/duplex |
| `NICRAM` | Нет прямого аналога | Не входит | У 3C509B FIFO, а не доступная packet RAM; покрывается `EL3LB` |
| `NICLB` | `EL3LB` | Только IMG | FIFO/controller loopback |
| `NICTX` / `NICRX` | `EL3TX` / `EL3RX` | Только IMG | Физические Ethernet-тесты |
| `ARP` | `ARP` | Только IMG | ARP probe/cache diagnostic |
| `PING` | `PING` | IMG + ZIP | Пользовательская утилита |
| `PINGALT` | `PINGALT` | Только IMG | Независимый диагностический путь/регрессия |
| `UDPTEST` | `UDPTEST` | Только IMG | UDP echo/generator |
| `TFTP` | `TFTP` | IMG + ZIP | GET/PUT |
| `NETCFG` | `NETCFG` | IMG + ZIP | Единственный reader `NET.CFG` |
| `IFUP` | `IFUP` | IMG + ZIP | Static/DHCP bring-up |
| `NSLOOKUP` | `NSLOOKUP` | IMG + ZIP | DNS A queries |
| `NTP` | `NTP` | IMG + ZIP | Raw UDP NTP и установка DSS clock |
| `WGET` | `WGET` | IMG + ZIP | Plain HTTP download/resume |
| `FTP` | `FTP` | IMG + ZIP | Passive FTP с двумя TCP-каналами |
| `TELNET` | `TELNET` | IMG + ZIP | ANSI/VT100 и файловые передачи |
| `DLSPEED` | `DLSPEED` | Только IMG | End-to-end HTTP benchmark |
| `DLDIRECT` | `DLDIRECT` | Только IMG | Прямой RX/TCP benchmark без диска |
| `DLDIRCP` | `DLDIRCP` | Только IMG | Контрольная реализация прямого download path |
| `UNETRTL.DLL` / `UNETESP.DLL` | `UNET509B.DLL` | IMG + ZIP | UNET ABI v1.0 |
| `UNETTEST` | `UNETTEST` | Только IMG | Полный smoke/regression test DLL |
| ESP `NETUP`, `NETRESET`, `NETPROBE`, `WTERM` | Нет | Не входит | Специфично ESP-AT/UART и неприменимо к Ethernet-карте |

Удалить или заменить строку этой таблицы можно только с записанным техническим
обоснованием. Отсутствие прямого аналога не должно оставлять непроверенной
соответствующую возможность контроллера.

## 2. Критическое решение по IRQ

IRQ-линии ISA в реализации Sprinter сознательно не подключены. Это штатная
особенность платформы, а не дефект эмуляции.

- Не подключать `irq*_callback` 3C509B к CPU в MAME.
- Не добавлять аппаратную IRQ-маршрутизацию в Sprinter.
- Нельзя делать IRQ обязательным условием обнаружения или запуска карты.
- Драйвер обязан работать polling-методом с конечными тайм-аутами.
- Маски событий контроллера при инициализации устанавливаются так, чтобы карта
  не рассчитывала на обслуживание прерываний.
- Значение IRQ из EEPROM разрешено показывать в `EL3INFO` только как справочное
  поле с текстом `not used by Sprinter`.
- В `NET.CFG` и переменных окружения нет параметра настройки IRQ.
- IRQ-тесты отсутствуют во всех этапах и не являются условием выпуска.

## 3. Источники и приоритет достоверности

При расхождении источников используется следующий порядок:

1. [3Com EtherLink III Drivers Technical Reference, 09-0398-002B](https://www.ardent-tool.com/NIC/3c5x9b_Technical_Reference.pdf).
2. Поведение реальной 3C509B-TPO на Sprinter.
3. [3Com-3C509B-nestor-DOS-driver](https://github.com/AzureCloudMonk/3Com-3C509B-nestor-DOS-driver),
   зафиксированная ревизия
   [`20384c78615159b9c5162fb1b71d4a55940efbcc`](https://github.com/AzureCloudMonk/3Com-3C509B-nestor-DOS-driver/commit/20384c78615159b9c5162fb1b71d4a55940efbcc).
4. [Linux 3c509 driver documentation](https://www.kernel.org/doc/html/v6.6/networking/device_drivers/ethernet/3com/3c509.html)
   и [исходный код драйвера](https://github.com/torvalds/linux/blob/master/drivers/net/ethernet/3com/3c509.c).
5. Модель MAME после её проверки и исправления.

Локальные проекты для архитектуры, утилит и упаковки:

- `/Users/dmitry/dev/zx/sprinter/sprinter-rtl8019a`;
- `/Users/dmitry/dev/zx/sprinter/sprinter_wifi/network`;
- `/Users/dmitry/dev/zx/sprinter/mame`.

### 3.1. Правила использования Nestor DOS driver

Nestor driver важен как практически проверенный пример работы 3C509B в
восьмибитном ISA-слоте PC/XT. Проверяются следующие части:

- ID-port selection и LFSR activation sequence;
- чтение EEPROM и обнаружение адаптера;
- выбор I/O-базы и активация карты;
- переключение окон;
- инициализация TPO;
- последовательности RX/TX reset и enable;
- передача, приём и освобождение кадров;
- обработка RX Status и TX Status;
- восстановление после underrun, jabber и других ошибок.

Исходный packet driver основан на GNU GPL. Поэтому:

- исходный код не копируется в этот BSD-проект;
- сначала составляется таблица наблюдаемого поведения и псевдокод;
- реализация на Z80 пишется независимо по документации 3Com;
- x86 word I/O нельзя механически переносить: Sprinter выполняет явные
  low-byte/high-byte операции;
- interrupt-driven часть DOS packet driver не используется;
- DOS-рекомендации по выбору IRQ к Sprinter не относятся;
- `3CCFG.EXE` и другие бинарные файлы из референса не включаются в дистрибутив.

## 4. Аппаратная модель и обязательные правила ISA8

### 4.1. Обнаружение и активация

- ID-порт выбирается из диапазона `0x100..0x1F0` с шагом `0x10`.
- Значение по умолчанию — `0x110`.
- После выбора ID-порта передаётся 255-байтовая LFSR-последовательность,
  начинающаяся с `0xFF`, полином `0xCF`.
- EEPROM читается через ID-порт до активации обычного I/O-окна.
- Поддерживаются I/O-базы `0x200..0x3E0` с шагом `0x10`.
- По умолчанию используется база из EEPROM; конфигурация может задать временную
  базу без записи EEPROM.
- Поддерживается один выбранный адаптер. Сценарий нескольких 3Com-карт в одном
  ISA-сегменте не входит в версию 1.
- Если classic activation не отвечает, программа завершает работу по тайм-ауту
  и сообщает о возможном PnP-состоянии карты. Зависание недопустимо.

### 4.2. Доступ к регистрам

- Для 16-битного регистра сначала выполняется доступ к младшему байту, затем
  немедленно к старшему.
- Между двумя байтами запрещены обращения к другим регистрам карты.
- Команда считается переданной после записи старшего байта command word.
- Для FIFO действуют отдельные правила: байтовый поток читается и пишется только
  через допустимый нижний FIFO-порт.
- Доступ через `base+1` и `base+3` как к старшим FIFO-байтам запрещён.
- Все ожидания `Command-In-Progress`, EEPROM busy, TX free и RX discard имеют
  конечный тайм-аут.

### 4.3. Передача

- `NETDRV.SEND_FRAME` принимает Ethernet II кадр без CRC длиной 14–1514 байт.
- Короткий кадр программно дополняется нулями до минимальных 60 байт; длина в
  TX preamble должна содержать уже дополненную длину. Это обязательно для ARP и
  других коротких кадров и не перекладывается на 3C509B.
- Перед кадром записывается четырёхбайтовый TX preamble контроллера: длина/флаги
  и нулевое второе слово.
- После эффективного кадра записывается нулевое дополнение до границы DWORD.
- Перед началом проверяется `TX Free` для preamble, эффективной длины и DWORD
  padding: `4 + align4(max(input_length, 60))`.
- TX Status читается для анализа и снимается предусмотренной документацией
  записью.
- Jabber и underrun приводят к TX reset, ожиданию CIP и повторному TX enable.
- Автоматический бесконечный retry запрещён; число попыток ограничивается.

### 4.4. Приём

- Сначала читается RX Status и длина пакета.
- Пакеты с ошибкой или длиной вне `60..1514` отбрасываются с диагностическим
  счётчиком.
- Принятый кадр целиком переносится из FIFO в обычный буфер до передачи верхнему
  уровню.
- Успешный `EL3.READ_FRAME` является consuming-операцией: он читает данные,
  выдаёт ровно одну команду `RX_DISCARD` и ожидает CIP перед возвратом.
- `EL3.DISCARD_FRAME` используется только для отбрасывания ещё не прочитанного
  текущего пакета, например после предварительно обнаруженного RX error.
- После успешного `READ_FRAME` вызывать `DISCARD_FRAME` запрещено: это удалит
  следующий пакет.
- Достижение конца данных не должно автоматически удалять пакет.
- Два кадра, стоящие в FIFO подряд, должны обрабатываться независимо.

### 4.5. Sprinter DSS

- ISA отображается через окно Sprinter `0xC000..0xFFFF`.
- На время открытого ISA-окна запрещены DSS-вызовы и доступ к системным страницам.
- Критическая секция ISA должна быть короткой; длинные тайм-ауты выполняются как
  серия коротких проверок с закрытием окна между итерациями.
- Командная строка через IX сохраняется до первого вызова, меняющего IX.
- Нулевые рабочие буферы EXE не включаются в файл и располагаются в BSS либо на
  выделенных DSS-страницах.
- Код и данные приложения не должны пересекать `0xC000`.
- Для DLL соблюдаются требования libman к размещению релокируемых данных.

## 5. Архитектура программного обеспечения

### 5.1. Структура исходников

```text
src/include/   регистры, структуры, ABI и общие константы
src/lib/       EL3, NETDRV, Ethernet, ARP, IPv4, ICMP, UDP, TCP, DHCP, DNS
src/apps/      пользовательские и тестовые программы
src/dll/       UNET509B.DLL
config/        примеры NET.CFG и CONNECT.BAT
docs/          пользовательская и разработческая документация
examples/      C- и Pascal-примеры
tools/         host-side тесты, серверы и упаковка
build/         промежуточные результаты, не для распространения
distr/         итоговые IMG и ZIP
```

### 5.2. Слои

```text
Приложения / UNET509B.DLL
        |
DHCP DNS NTP TFTP TCP HTTP FTP TELNET
        |
IPv4 / ICMP / UDP / ARP
        |
NETDRV — независимый интерфейс Ethernet-драйвера
        |
EL3 — 3C509B, окна, команды, FIFO, polling
        |
Sprinter ISA8
```

Верхние уровни из RTL8019AS разрешается переносить только после устранения прямых
вызовов `RTL.*`. Весь доступ к карте идёт через `NETDRV`.

### 5.3. Соглашение низкоуровневого ABI

- Успех: Carry сброшен.
- Ошибка: Carry установлен, в `A` находится код ошибки.
- `AF`, `BC`, `DE`, `HL` считаются изменяемыми, если функция не оговорена иначе.
- `IX` и `IY` сохраняются публичными функциями библиотеки.
- Указатель на буфер передаётся в `HL`, размер или ёмкость — в `BC`.

Обязательные функции `EL3`:

| Функция | Назначение |
|---|---|
| `EL3.DETECT` | Найти и идентифицировать карту, заполнить context |
| `EL3.ACTIVATE` | Активировать выбранную I/O-базу без записи EEPROM |
| `EL3.READ_EEPROM` | Прочитать слово EEPROM; индекс в `A`, результат в `DE` |
| `EL3.SELECT_WINDOW` | Выбрать окно 0–6 |
| `EL3.READ16` | Атомарно прочитать регистр low/high |
| `EL3.WRITE16` | Атомарно записать регистр low/high |
| `EL3.CMD` | Передать command word |
| `EL3.WAIT_CIP` | Дождаться снятия Command-In-Progress |
| `EL3.RESET` | Global, RX или TX reset с нужной задержкой |
| `EL3.INIT` | Полностью подготовить карту к polling RX/TX |
| `EL3.SEND_FRAME` | Отправить кадр `HL`, `BC` |
| `EL3.RX_PENDING` | Вернуть в `A`: 0 — пусто, 1 — пакет готов |
| `EL3.READ_FRAME` | Перенести и потребить кадр, вернуть длину в `BC` |
| `EL3.DISCARD_FRAME` | Потребить текущий кадр без копирования |
| `EL3.SNAPSHOT` | Заполнить версионированную диагностическую структуру |
| `EL3.DONE` | Остановить RX/TX и освободить ресурсы |

Обязательные функции `NETDRV`:

- `NETDRV.INIT`;
- `NETDRV.DONE`;
- `NETDRV.SEND_FRAME`;
- `NETDRV.RX_PENDING`;
- `NETDRV.READ_FRAME`;
- `NETDRV.DISCARD_FRAME`;
- `NETDRV.SNAPSHOT`.

Для `READ_FRAME` входной `BC` — ёмкость буфера. При успехе выходной `BC` — длина
кадра. Если буфер мал, функция возвращает `E_FRAME_SIZE`, сообщает требуемую
длину через context/snapshot и сама выполняет один `RX_DISCARD`. Таким образом,
ни один публичный путь не оставляет ошибочный пакет в голове FIFO и не допускает
двойного удаления.

Минимальные коды ошибок:

- `E_NO_CARD`;
- `E_BAD_PRODUCT`;
- `E_BAD_CONFIG`;
- `E_EEPROM_TIMEOUT`;
- `E_CMD_TIMEOUT`;
- `E_TX_TIMEOUT`;
- `E_TX_ERROR`;
- `E_RX_ERROR`;
- `E_FRAME_SIZE`;
- `E_LINK_DOWN`;
- `E_NO_MEMORY`.

Диагностический snapshot содержит версию структуры, ISA-слот, ID-порт, I/O-базу,
MAC, product ID, последние command/status/RX status/TX status и счётчики reset,
timeout, RX error, TX error, dropped и received/transmitted frames.

### 5.4. UNET509B ABI

- `src/include/unet.inc` переносится побайтно из общего UNET ABI v1.0; сборка
  должна падать при несовпадении с эталонным include соседнего проекта.
- DLL имеет DOS-имя `UNET509B.DLL`, libman target 1.3/L1 и backend tag `509B`.
- Capability mask версии 1: `0x001F` — TCP, UDP, RESOLVE, PING, MULTICHAN.
- Поддерживаются два одновременных channel context для FTP control/data.
- LISTEN, RAWETH, RXFLOW и ASYNCSEND не заявляются.
- `RXPAUSE` и `RXRESUME` возвращают success как no-op; аппаратное FIFO всё равно
  обслуживается polling-кодом при следующем вызове API.
- `SETOPT RXTRIG` и иные аппаратно неприменимые настройки возвращают
  `NERR_NOTSUP`.
- GETINFO возвращает общие сетевые поля, backend `509B` и hardware descriptor
  `slot/#base`; SSID и BAUD возвращаются пустыми.
- LASTERR содержит стабильный код драйвера и краткий диагностический текст без
  указателей во временные ISA/DSS-окна.
- C/Pascal bindings не содержат имени конкретной DLL в API: имя backend DLL
  передаёт приложение или выбирает launcher по `NET=509B`.

## 6. Конфигурация сети

Единственным приложением, читающим `NET.CFG`, является `NETCFG.EXE`. Остальные
программы используют опубликованные переменные окружения.

Комплект не устанавливает TSR и не оставляет резидентный сетевой драйвер.
Каждая утилита самостоятельно выполняет `EL3/NETDRV.INIT`, использует сеть и
вызывает `DONE`. Stage 7 `IFUP` каждый раз получает новый DHCP lease и публикует окружение, но
после выхода не обслуживает ARP или RX. Поэтому запуск очередной программы не
может зависеть от сохранённого аппаратного состояния предыдущей.

Пример:

```ini
NET=509B
HW=AUTO
IDPORT=#110
MAC=

IP=DHCP
NETMASK=
GATEWAY=
DNS1=
DNS2=
NTP=pool.ntp.org
TZ=+4
```

Правила:

- `NET` — селектор аппаратного L2-backend, а не название всего IPv4/TCP-стека.
  Значение `509B` однозначно выбирает этот драйвер и позволяет launcher собрать
  DOS-имя `UNET` + `509B` = `UNET509B.DLL`.
- Сокращение `3C5` запрещено как неоднозначное: 3Com 3C503 относится к EtherLink
  II и использует NS8390 с отдельной board-specific обвязкой, тогда как 3C509B —
  EtherLink III с оконными регистрами и FIFO. См. [EtherLink II reference](https://bitsavers.org/communications/3Com/4185-01_EtherLink_II_Technical_Reference_Jan89.pdf)
  и [Linux 3C503 driver](https://github.com/spotify/linux/blob/master/drivers/net/3c503.c).
- `NET=509B` обязателен; другое значение означает конфигурацию иного backend и
  отклоняется этим комплектом до обращения к ISA.
- `HW=AUTO` означает поиск по ISA-слотам и использование EEPROM I/O-базы.
- Явная форма `HW=1/#300` задаёт слот и временную I/O-базу.
- `IDPORT` задаёт classic configuration port; допустимы
  `#100..#1F0` с шагом `#10`.
- Пустой `MAC` означает MAC из EEPROM; явный MAC предназначен для стенда.
- `HW`, `IDPORT` и `MAC` не получают backend-префикс: их область уже однозначно
  задаёт `NET`. Backend выбирается до разбора и проверки этих значений.
- `IP=DHCP` включает DHCP.
- Для static mode в `IP` указывается адрес, а `NETMASK` обязателен.
- `CONNECT.BAT` последовательно запускает `NETCFG` и `IFUP`.
- `NETCFG -i` предназначен также для `AUTOEXEC.BAT`; приложения не перечитывают
  конфигурационный файл во время сетевой операции.
- Неизвестные ключи дают предупреждение; некорректные обязательные значения —
  ошибку и ненулевой код возврата.
- IRQ-параметр отсутствует.

Публикуемые переменные:

```text
NET=509B
NET_HW
NET_IDPORT
NET_MAC
NET_IP_SRC
NET_IP
NET_MASK
NET_GW
NET_DNS1
NET_DNS2
NET_NTP
NET_TZ
NET_DHCP_SRV
NET_LEASE_SEC
```

`NET_HW`, `NET_IDPORT` и `NET_MAC` являются нормализованными значениями
соответствующих ключей. Верхние сетевые уровни читают только общие `NET_*`
переменные и не знают, какой backend их опубликовал. Специфичный для 3C509B смысл
`IDPORT` документируется backend, но отдельного namespace для него не требуется.

### 6.1. Общий синтаксис CLI

- Короткие флаги с `-` и `/` равнозначны и не зависят от регистра.
- Help: `/?`, `-?` или `-h`.
- Длинные GNU-флаги не используются.
- Общий parser находится в `src/lib/cmdline_lib.asm` и предоставляет разбор
  positional arguments, IPv4, порта, `u16`, `u32` и размера файла.
- Первая инструкция приложения, использующего параметры, сохраняет IX как
  источник PSP command line; вычислять PSP через фиксированный адрес запрещено.
- Все пользовательские команды и примеры фиксируются в `USAGE.md`, который при
  упаковке преобразуется в `USAGE.TXT`.

Обязательные формы настройки:

```text
NETCFG               показать опубликованные NET_* значения
NETCFG -i            прочитать NET.CFG и опубликовать окружение
NETCFG -c            проверить синтаксис без изменения окружения
NETCFG -d            удалить опубликованные NET_* значения
NETCFG -v            подробно проверить файл и карту без публикации
IFUP                 static init либо DHCP acquire
```

`NETCFG -v` также допустим как модификатор `NETCFG -i -v` и `NETCFG -c -v`.
Renewal, RELEASE, `IFUP -r`, `IFUP -d` и `NET_STATE` не входят в Stage 7.

Обязательные формы пользовательских сетевых программ:

```text
EL3INFO [-v]
PING [-t] [-n count] [-l size] [-i ttl] [-w milliseconds] target
NSLOOKUP name [dns-server]
NTP [server]
TFTP host[:port] GET remote [-o local] [-y|-f]
TFTP host[:port] PUT local [-o remote]
WGET url [-o output] [-y|-f] [-r] [-d]
FTP host[:port] remote [-o local] [-u user] [-p pass] [-y|-f] [-r] [-d]
FTP host[:port] PUT local [-o remote] [-u user] [-p pass]
FTP host[:port] [path] -l|-n [-u user] [-p pass]
TELNET host[:port]
TELNET host port
```

`-y` и `-f` равнозначно разрешают перезапись. `-r` означает resume и имеет
приоритет над `-y`/`-f`. У WGET `-d` заменяет KB-counter одной точкой на каждую
8-КиБ запись; финальный time/speed summary сохраняется. Ключи WGET принимают
префикс `-` или `/`, не зависят от регистра и могут идти в любом порядке.
PING по умолчанию отправляет четыре пакета с 32-байтовым payload, TTL 64 и
тайм-аутом 1000 мс; максимальный payload — 1472 байта.

### 6.2. Коды завершения

Все EXE используют единый набор DSS exit codes в регистре `B`:

```text
0  успех
1  ошибка синтаксиса/аргументов
2  карта не найдена или аппаратная ошибка
3  сетевая ошибка или timeout
4  отсутствующая/некорректная конфигурация
5  локальная ошибка DSS/файла/памяти
6  ошибка удалённого протокола или сервера
7  операция отменена пользователем
```

Общие функции `UTIL.EXIT`, `UTIL.EXIT_OK` и `UTIL.EXIT_FAIL` завершают процесс и
печатают машинно распознаваемую строку `RESULT OK` либо `RESULT FAIL code=N`.

### 6.3. Начальные тайм-ауты и повторы

Низкоуровневые операции EL3 этапов 3–6 измеряются квантом `CYCLES21`: не менее
21023 T-state, то есть не менее 1 мс при 21 МГц и около 6 мс при 3,5 МГц.
`Command-In-Progress` и EEPROM используют 100 waitq, FIFO/TX/RX — 1000 waitq;
реальное время поэтому находится в диапазоне 1×–6× от номинала. Будущие
сетевые и интерактивные ожидания используют DSS ticks/монотонное время. Ни один
из этих тайм-аутов не является неограниченным числом Z80-итераций.

```text
Global/RX/TX reset        100 CYCLES21 waitq
Command-In-Progress       100 CYCLES21 waitq
EEPROM command/busy       100 CYCLES21 waitq
TX FIFO/complete          1000 CYCLES21 waitq
RX FIFO/discard           1000/100 CYCLES21 waitq
EL3TX/EL3RX link/RX wait  10000 CYCLES21 waitq (примерно 10–60 с)
ARP                       2000 ms, 3 попытки
UDP/DNS/NTP reply         5000 ms, 3 попытки
TFTP block                5000 ms, 6 попыток
DHCP initial              4000 ms, 4 попытки с backoff до 16000 ms
TCP SYN                   1700 ms, 3 попытки (около 5.1 s суммарно)
TCP data retransmit       1000, 2000, 4000 ms
TCP FIN/close             5000 ms
HTTP/FTP idle             15000 ms
```

Прерывание ожидания клавишей разрешено только вне открытого ISA-окна. Ни один
timeout не должен зависеть от наличия IRQ.

### 6.4. Формат диагностики

Каждая программа печатает имя, версию и короткие stage codes. Подробные коды
скрыты флагом `-v`, кроме фатальной ошибки. Пример:

```text
3C509B EL3INFO v0.0.1
[E0] SLOT=1 IDPORT=0110
[E1] PRODUCT=9550 IO=0300
[E2] MAC=00:10:5A:12:34:56
[E3] IRQ=10 (not used by Sprinter)
RESULT OK
```

При timeout выводятся stage code, elapsed ticks, slot/base, command/status,
RX/TX status, счётчики и целевой IP/MAC/port, если он есть. Диагностика не должна
требовать от пользователя отладчик MAME.

### 6.5. Память и буферы

- RX и TX work buffers рассчитаны минимум на 1518 байт и находятся в BSS/DSS
  pages, а не как нулевые данные внутри EXE.
- ARP cache: четыре записи с age/lifetime.
- TCP: два channel contexts с раздельными sequence/window/retry состояниями.
- Начальный TCP MSS: 536 байт; увеличение допускается только после тестов памяти
  и производительности в обеих средах.
- TCP `SEND` принимает caller-owned буфер до доступной ёмкости приложения и
  прозрачно делит его на сегменты не больше effective MSS. Поэтому нативное
  ограничение MSS 536 совместимо с sibling API и не ограничивает операцию
  отправки одним сегментом.
- Приёмное окно (`TCP_RECV_WINDOW`, `src/include/tcp.inc`) — кратное MSS число
  сегментов, которые пир может держать в полёте без ожидания ACK; некратное
  окно останавливает канал при каждом исчерпании (проверено на коде сиблинга
  RTL8019A). Для WGET (`STAGE12_LAYOUT`) это 5×MSS = 2680 байт, как и у
  сиблинга; для остальных клиентов (два канала, TCPTEST) окно остаётся 1×MSS,
  потому что странице не хватает места на второй расширенный приёмный буфер на
  канал. Потолок задаёт не страница, а карта: анонсированное окно может прийти,
  пока приложение занято другим (сбрасывает файловый буфер на диск), и до
  следующего `READ_FRAME` всё это лежит в RX FIFO самого 3C509B.
- **Анонсируется реально свободное место**, `CAPACITY - PENDING_OFF -
  PENDING_LEN` — то же число, против которого accept-путь `PROCESS_TCP`
  проверяет сегмент, поэтому каждый обещанный байт действительно будет принят.
  Раньше на его месте стоял двоичный признак «полное окно или ноль»: он
  объявлял ноль, как только в очереди оказывался хоть один сегмент, то есть
  **каждый ACK на принятые данные закрывал окно**. Пир останавливался после
  каждого MSS, на переоткрытие уходил ещё один round trip, а поскольку window
  update отправляется best-effort и без повтора, любая его потеря стоила пиру
  persist-timer backoff 1/2/4 с. Измерено в EXE-гарнессе на 70001 байте: 266
  кадров от клиента на 131 сегмент данных против 135 после исправления, шагов
  2 068 551 → 1 706 245. Меньше одного MSS по-прежнему анонсируется как ноль
  (silly window syndrome), и защёлка `CTX_WINDOW_CLOSED` — единственное, что
  заставляет `RECV` слать явное переоткрытие. Область линейная, поэтому голова,
  оставшаяся после частичного вычитывания, не предлагается снова до полного
  опустошения: это стоит одного window update на заполненную область вместо
  одного на сегмент и держит обещание точным, ничего не перекладывая.
- Каждый принятый сегмент по-прежнему подтверждается
  немедленно: попытка отложенного/накопительного ACK ловила
  `@NETDRV.RX_PENDING`, вызванный изнутри `PROCESS_FRAME`, и портила состояние
  приёма на закачках больше ~40 КБ (см. `PENDING_CAPACITY`/`ACK_IF_DATA` в
  `tcp_transport.asm`); причина не установлена, функция отключена. Передача
  (`SEND`) остаётся stop-and-wait, как и у сиблинга.
- Горячие циклы приёма переписаны под бюджет T-состояний, без изменения
  протокола: `ETHERNET.ACCUMULATE` держит сумму в `HL`, а источник в `DE`
  (21 T на байт вместо 66 T, которые стоила адресация `(IX+0)`), а
  `EL3IO.FIFO_READ` считает через `DJNZ` при неподвижном указателе порта
  (33 T на байт вместо 53). Перенос переноса через весь буфер требует, чтобы
  управление циклом состояло только из инструкций, не трогающих CF, а сложение
  младшей половины слова первым — чтобы перенос попадал в бит 0 следующего
  слова (end-around carry RFC 1071), из-за чего сумма в цикле хранится
  побайтово переставленной. Измерено на эталонной закачке 70001 байта в
  EXE-гарнессе: 2 664 083 → 2 052 461 шага (−23%), на 200001 байте
  7 412 690 → 5 668 317 (−23%); потолок закреплён проверкой в
  `tools/test-stage12-exe.js`, корректность — векторами RFC 1071 в
  `tools/stage7_vectors.asm` (чётная/нечётная длина, насыщение `FFFF`,
  буфер длиннее 256 слов). После исправления анонса окна та же закачка стоит
  1 706 245 шагов; бюджет в тесте — 1 900 000, плюс отдельная проверка глубины
  приёмного конвейера (`maxInFlight >= 5×MSS`), потому что шаги CPU её не
  видят. Важно, что процессор здесь и не был узким местом: 1,7 млн тактов на
  21 МГц — это около 0,08 с на 70 КБ, тогда как наблюдаемая закачка занимала
  секунды. Ограничение — round-trip задержки, поэтому решает глубина окна,
  а не развёртка циклов.
- Тестовые пиры обязаны уметь заполнять анонсированное окно. И
  `tools/exe-harness/harness.js`, и `tools/host/stage12_responder.py` раньше
  отвечали одним сегментом на каждый принятый кадр — строгий stop-and-wait,
  в котором никакое приёмное окно не может себя проявить, поэтому оптимизация
  не была видна ни в одном тесте и ни в одном прогоне MAME. Оба шлют сегменты,
  пока `window - in_flight` позволяет; гарнесс публикует пиковый `maxInFlight`,
  а `tools/host/test_stage12_responder.py` требует пять сегментов на один
  запрос при окне 5×MSS.
- HTTP/TFTP/FTP пишут на диск блоками; WGET использует непрерывный файловый
  буфер 6 КБ на DSS-странице и общий 2048-байтный RX/TX work area. Два КБ,
  которые буфер отдал, ушли в приёмное окно: батчинг записи экономит только
  вызовы DSS, а окно определяет, сколько сегментов пир держит в полёте. DNS query и
  retransmit payload восстанавливаются перед повторной отправкой; обработчик
  входящего ARP безопасно сохраняет нужные исходные поля.
- RX-кадр сначала полностью вычитывается из аппаратного FIFO, после чего верхние
  уровни могут работать с ним без открытого ISA-окна.
- Stage 7 EXE выделяют одну DSS-страницу и приватно отображают её в WIN1: TX
  начинается с `#4000`, RX — с `#4800`; compile-time assertions запрещают
  пересечение и выход за `#C000`. Эти адреса не входят в NETDRV ABI.
- Адреса страницы задаются через `PAGE_BASE` — окно, в которое программа
  отображает свою страницу. По умолчанию `#4000`: образ загружается по `#8080`,
  занимает WIN2 и маппит страницу в WIN1. Исключение — WGET: его образ занимает
  целое окно, поэтому он загружается по `#4080` в WIN1 и забирает WIN2
  (`PAGE_BASE = #8000`), как это делают большие утилиты RTL-кита. Стек в обоих
  случаях обязан оказаться в отображаемом окне, а не в WIN1: BIOS `WIN_MOVE`
  накладывает видеостраницу на WIN1 при каждой прокрутке консоли и
  восстанавливает SLOT1 через `POP`, взятый пока эта страница ещё отображена,
  поэтому стек в WIN1 не переживает первой прокрученной строки.
- Стек из заголовка EXE (слово по смещению 20) DSS устанавливает до входа в
  программу и тратит на загрузчик и кадры прерываний, поэтому он обязан быть
  свободен от образа ещё до первой инструкции: `LD SP` в точке входа уже поздно.
  Для WGET это `#8000` — вершина окна образа, под которой compile-time assertion
  держит свободными `S10_BOOTSTRAP_STACK_RESERVE` байт. Пока стек стоит там, он
  находится в WIN1, поэтому до захвата страницы запрещён любой вывод на консоль.
- WGET использует **штатную раскладку DSS**: программа владеет WIN1+WIN2 как
  одной непрерывной областью `#4100..#BFFF`, отдельная страница не запрашивается.
  Поэтому оба стека стоят на `#BFF0` (вершина WIN2, целое окно выше образа),
  `GETMEM`/`SETWIN2`/`FREEMEM` для Этапа 12 не нужны, а `ALLOCATE_FRESH` вместо
  свежей страницы обнуляет область данных от `PAGE_BASE` до копии командной
  строки. Границей образа служит начало области данных: `ASSERT $ <= PAGE_BASE`
  в `src/apps/wget.asm`, продублированный в `tools/test-stage12-asm.sh`,
  `tools/check-stage12.pl` и `tools/test-stage12-exe.js`. Потолок образа — 16256
  байт.
- До этого WGET использовал вариант «Page2» (образ только в WIN1 плюс claim
  отдельной страницы в WIN2), и входной стек стоял на `#8000` — вплотную над
  образом. Любой образ, заканчивающийся выше `#7F40`, при этом собирался,
  загружался, доходил до `ESTABLISHED` и терял `CTX_SND_NXT` на первом же
  сегменте данных, отваливаясь по `TCP_ERR_TIMEOUT`. Граница измерена побайтово:
  16064 работает, 16065 нет. Отказ выглядел как ошибка TCP и смещался от любых
  посторонних правок, поэтому дважды расследовался как дефект стека протокола.
  Единственное, ради чего нужен был Page2 — возможность позже перекладывать
  WIN2 — не использовалось ни разу.
- Если 32 КБ (WIN1+WIN2) когда-нибудь кончатся, следующий шаг — схема
  WIN0/PRELOAD из `sdcc-sprinter-sdk/lib/win0/` (~47 КБ: код в WIN0+WIN1,
  трамплины `RST #08`/`#10`/IM1 в WIN2, двухстадийный загрузчик через поле
  `LOADER` в заголовке EXE), а не уменьшение какого-либо резерва.
- NETDRV принимает caller-owned pointer/length и проверяет начало, конец,
  16-битный overflow и границу `#C000`. Для будущей DLL буфер также обязан быть
  целиком вне окна загрузки DLL.
- Общий UNET contract разрешает загрузку DLL только в WIN1 или WIN2; WIN3
  `#C000..#FFFF` зарезервирован для ISA. Остаётся не менее 256 байт свободного
  стека, библиотека нерентерабельна. Stage 7 DLL ещё не реализует.

## 7. MAME: объём работ и критерии достоверности

Исходная ветка MAME содержит начальную модель 3C509B. Существующие изменения
нельзя удалять или переписывать без аудита. До признания модели пригодной нужно
проверить и при необходимости исправить:

- полный диапазон I/O-баз, а не сокращённую таблицу;
- корректное EEPROM-кодирование I/O-базы;
- low/high-byte latch для 16-битных регистров;
- запрет FIFO-доступа через старшие байтовые offsets;
- TX padding до DWORD, а не только до WORD;
- явное удаление RX-пакета командой `RX_DISCARD`;
- сохранение следующего пакета при back-to-back RX;
- снятие TX Status записью, а не чтением;
- глубину TX Status stack согласно документации;
- направление сравнения RX Early threshold;
- reset delays и Command-In-Progress;
- station address byte order;
- сохранение состояния для save state.

Отсутствие связи `irq*_callback` с CPU Sprinter является правильным поведением и
не подлежит исправлению.

MAME запускается с картой в ISA-слоте и pcap backend. Доступные интерфейсы
проверяются через `-listnetwork`; точное имя интерфейса записывается в локальный
тестовый профиль, но не зашивается в исходники. См. [MAME network options](https://docs.mamedev.org/commandline/commandline-all.html).

## 8. Поэтапная разработка

### Этап 0. Основа проекта и сборка

Результат: воспроизводимый пустой каркас, запускающий минимальную DSS-программу.

- [x] Создать каталоги из раздела 5.1.
- [x] Добавить BSD-3-Clause `LICENSE`.
- [x] Настроить sjasmplus и Makefile.
- [x] Проверять зависимости сборки: sjasmplus, mtools, zip, iconv и Perl либо
  эквивалентный проверенный конвертер Markdown/plain-text.
- [x] Реализовать цели `make build`, `make test-host`, `make image`, `make package` и `make clean`.
- [x] Добавить единый manifest артефактов, отдельно для IMG и ZIP.
- [x] Создать `tools/artifacts.sh`, `tools/image.sh` и `tools/package.sh` по
  проверенной схеме соседнего RTL-проекта.
- [x] Создать `HELLO.EXE` без сетевого кода.
- [x] Проверить границу `0xC000`, размер EXE и отсутствие в файле больших нулевых BSS.
- [x] Запустить `HELLO.EXE` в MAME.
- [ ] Запустить `HELLO.EXE` на реальном Sprinter.
- [x] Зафиксировать версии sjasmplus 1.22.0, MAME 0.287 (LP64) и DSS 1.71.64.
- [x] Добавить лог сборки и запусков: `docs/evidence/README.md`.
- [x] Критерий этапа: чистая сборка создаёт запускаемый `HELLO.EXE`.

### Этап 1. Аудит документации и референсов

Результат: таблица поведения контроллера до написания драйвера.

- [x] Извлечь из документации 3Com окна, регистры, команды и timing requirements.
- [x] Проверить Nestor driver на зафиксированной ревизии.
- [x] Составить таблицу `операция / 3Com / Nestor / Linux / MAME`.
- [x] Описать псевдокод ID sequence, EEPROM read, activation, INIT, TX, RX и recovery.
- [x] Отметить GPL-фрагменты как непереносимые.
- [x] Отметить IRQ-код как неприменимый к Sprinter.
- [ ] Проверить product ID и EEPROM реальной 3C509B-TPO внешними средствами.
- [ ] Записать исходные I/O, PnP, MAC и resource settings карты без их изменения.
- [x] Добавить отчёт аудита: `docs/STAGE1_AUDIT.md`
- [x] Критерий этапа: все команды, нужные этапам 3–6, имеют документированную последовательность.

### Этап 2. Исправление и проверка MAME

Результат: модель, достаточно точная для дальнейшей разработки polling-драйвера.

- [x] Реализовать весь диапазон I/O-баз `0x200..0x3E0`.
- [x] Исправить EEPROM address configuration.
- [x] Реализовать корректные ISA8 low/high semantics.
- [x] Исправить FIFO byte access.
- [x] Исправить TX DWORD padding.
- [x] Исправить RX packet lifetime и явный `RX_DISCARD`.
- [x] Исправить TX Status stack и pop operation.
- [x] Проверить reset, CIP, thresholds и filters.
- [ ] Добавить C++- или headless-регрессии для затронутой модели.
- [x] Убедиться, что IRQ не подключён к CPU.
- [x] Собрать MAME и проверить `-isa1 3c509b -listdevices`.
- [ ] Запустить MAME с pcap backend.
- [ ] Добавить воспроизводимые MAME regression scripts и логи: в
  `../mame/MAME_3C509B.md` перечислены `accuracy_probe.lua`, `base_probe.lua` и
  `savestate_probe.lua`, но в worktree эти файлы отсутствуют.
- [ ] Критерий этапа: известные ISA8/FIFO-расхождения устранены, регрессии проходят.

### Этап 3. ID-последовательность и EEPROM

Результат: карта надёжно находится и активируется без изменения EEPROM.

- [x] Реализовать выбор ID-порта.
- [x] Реализовать и проверить 255-байтовую LFSR-последовательность.
- [x] Реализовать EEPROM busy polling с тайм-аутом.
- [x] Прочитать MAC, product ID, address configuration и resource configuration.
- [x] Проверять product ID `0x9550` для TPO.
- [x] Реализовать активацию по EEPROM-базе.
- [x] Реализовать временную активацию по явно заданной базе.
- [x] Реализовать безопасный выход при отсутствии карты и PnP wait state.
- [x] Создать `EL3INFO.EXE`.
- [x] Создать тестовый полный дамп `EL3EEP.EXE` без функций записи.
- [x] Создать `ISAPROBE.EXE`: без аргументов только помощь; чтение выполняется
  лишь для явно заданных slot/base/count и никогда не запускает команды карты.
- [ ] Проверить в MAME оба ISA-слота и несколько допустимых I/O-баз.
- [ ] Проверить на реальной карте и сравнить MAC/ID/конфигурацию.
- [ ] Проверить отсутствие карты и неверный ID-порт.
- [ ] Добавить логи MAME/оборудования: ____________________
- [ ] Критерий этапа: `EL3INFO` одинаково идентифицирует карту в обеих средах.

### Этап 4. Регистровый слой и инициализация

Результат: карта переходит в стабильное polling-состояние RX/TX.

- [x] Реализовать атомарные `READ16` и `WRITE16`.
- [x] Реализовать `CMD`, `WAIT_CIP` и переключение окон 0–6.
- [x] Реализовать Global, RX и TX reset с задержками.
- [x] Записать station address в Window 2 с правильным порядком байтов.
- [x] Настроить TPO, link beat и jabber protection.
- [x] Настроить RX filter: individual + broadcast.
- [x] Отключить RX Early и ненужные thresholds.
- [x] Обнулить card interrupt/status masks, не подключая IRQ.
- [x] Включить RX и TX.
- [x] Реализовать `SNAPSHOT` и счётчики ошибок.
- [x] Создать `EL3REG.EXE`.
- [ ] Сравнить снимки регистров MAME и реальной карты.
- [ ] Проверить повторный INIT/DONE и recovery после timeout: MAME `-n 100` и
  принудительный mock-CIP timeout прошли; аппаратная проверка остаётся открытой.
- [ ] Добавить file-backed логи: MAME-вывод подтверждён пользовательскими
  снимками экрана 2026-08-30, bundle в `docs/evidence/` ещё не добавлен.
- [ ] Критерий этапа: 100 последовательных INIT/DONE без зависания и расхождения
  состояния: MAME PASS, реальный Sprinter/3C509B-TPO ожидается.

Зафиксированный результат MAME Stage 4 от 2026-08-30:

- `R01`–`R12`: оба ISA slot, `AUTO`, базы `#200/#300/#3E0`, `-n 1`, `-n 100`
  и отсутствие карты прошли; no-card возвращает конечный `code=3`.
- Границы CLI `-n 0/101`, `-s 2`, `-p #101`, `-b #3F0` возвращают `code=2`
  до ISA-доступа.
- Snapshot v1: `len=60`, `window=1`, `status=#2000`, filter `#0005`, masks
  `#0000/#0096`, thresholds `#07FC/#07FC/#0600`.
- Сто циклов дают `CYCLES OK=100`, `RESULT OK` и reset counter `#00C8`.
- Проверенный IMG SHA-256:
  `579b4acff658a90cd5bbb5181e35d917cf244e0d379c49e4b5993d1fa0da20ee`;
  `EL3REG.EXE` SHA-256:
  `5c00e9e824e3925b8b7498959bc8db34c5f774938c791badaa0656fdd39a7b07`.
- Локально `make test-host package image`, реальные Stage 3/4 ASM vectors,
  Bash/Perl syntax и `git diff --check` прошли. Версия остаётся `0.0.1`.

### Этап 5. FIFO и внутренний loopback

Результат локальной реализации: байтовая TX/RX-передача и выравнивание
подтверждены executable mock; MAME- и аппаратная приёмка остаются открытыми.

- [x] Реализовать вычисление TX required bytes.
- [x] Реализовать preamble и DWORD padding.
- [x] Проверить software padding входных кадров 14, 42 и 59 байт до 60 байт.
- [x] Реализовать polling TX completion и очистку TX Status.
- [x] Реализовать чтение RX Status и длины.
- [x] Реализовать перенос RX в обычный буфер.
- [x] Реализовать ровно один `RX_DISCARD` на пакет.
- [x] Создать `EL3LB.EXE`.
- [x] Проверить кадры 60, 61, 62, 63 и 1514 байт в executable mock.
- [x] Проверить шаблоны `00`, `FF`, `55`, `AA` и возрастающий счётчик
  в коде диагностической матрицы.
- [x] Проверить очереди из 2 и 10 различимых пакетов в executable mock и
  диагностике, чтобы потеря, дублирование или перестановка не маскировались.
- [x] Проверить underrun, jabber, bad RX status и recovery в executable mock;
  MAME fault injection отсутствует и остаётся открытым blocker.
- [ ] Выполнить одинаковую матрицу в MAME и на реальной карте.
- [x] Добавить процедуру и шаблон evidence:
  `docs/STAGE5_TESTING_RU.md`, `docs/evidence/STAGE5_TEST_TEMPLATE.md`.
- [ ] Добавить MAME/аппаратные логи/дампы: ____________________
- [ ] Критерий этапа: все длины и шаблоны проходят без потери следующего пакета.

Согласованное исключение порядка 5→6: ручная Stage 5 до реализации Stage 6 не
требуется. Сначала реализуются actual-EXE harness и Stage 6 и собирается
финальный IMG, затем Stage 5 и Stage 6 проверяются одним ручным MAME-сеансом.
Это исключение не закрывает ни один этап без evidence. По общему правилу
недоступность реального оборудования больше не блокирует последующую разработку;
hardware evidence остаётся открытым.

### Этап 6. Физические TX и RX

Результат: обмен Ethernet II кадрами с внешним компьютером.

- [x] Создать `EL3TX.EXE` для заданного MAC, EtherType, длины и шаблона.
- [x] Создать `EL3RX.EXE` с кратким и hex-dump режимами.
- [ ] Отправить unicast и broadcast из Sprinter и записать pcap.
- [ ] Отправить unicast и broadcast в Sprinter.
- [ ] Проверить отбрасывание чужого unicast.
- [ ] Проверить минимальный и максимальный кадр.
- [ ] Проверить back-to-back burst.
- [ ] Проверить link down/up и конечный тайм-аут.
- [ ] Сверить порядок MAC, EtherType, payload и padding.
- [ ] Убедиться, что CRC не передаётся верхнему уровню.
- [ ] Добавить pcap MAME/оборудования: ____________________
- [ ] Критерий этапа: кадры в обе стороны побайтно совпадают с ожидаемыми.

Локальная реализация и host evidence от 2026-08-31:

- `EL3TX [-v] [-s 0|1] [-p #100..#1F0] [-b AUTO|#200..#3E0] -d MAC
  [-t #0600..#FFFF] [-l 14..1514] [-f 00|FF|55|AA|INC] [-n 1..100]
  [-w 1..10000]`;
- `EL3RX [-v] [-x] [-s 0|1] [-p #100..#1F0] [-b AUTO|#200..#3E0]
  [-n 1..100] [-w 1..10000]`;
- defaults: EtherType `#88B5`, length 60, pattern `INC`, count 1, timeout
  10000. `-w` измеряется квантами `CYCLES21` (примерно 1–6 мс каждый), а не
  точными миллисекундами;
- `EL3_ERR_LINK_TIMEOUT=20`, `LINK_STATE` и bounded `WAIT_LINK_UP` реализованы;
  публичные функции сохраняют IX/IY, а provider закрывает ISA на каждом
  возврате;
- TX использует factory MAC как source, network-order EtherType, software
  padding до 60 и меняет фазу `INC` номером burst-кадра;
- RX оставляет individual+broadcast filter и печатает MAC/type/length и
  software IEEE CRC32 данных без FCS; `-x` печатает полный hex/ASCII dump;
- обе EXE используют одну DSS-страницу (`TX=#4000`, `RX=#4800`), всегда
  пытаются выполнить `DONE` и `FREEMEM`, сохраняя первичную ошибку;
- actual-EXE harness на MIT Z80 core выполняет те же бинарники, которые входят
  в IMG. Пройдено 119 проверок: `EL3LB -n 1` (52 кадра), slot/base/no-card/CLI,
  все Stage 6 длины/шаблоны и точные preamble/data/padding, TX/RX burst 2/10,
  RX discard, individual/broadcast/foreign unicast, immediate/delayed/down
  link, CRC32, EXE header/boundary и cleanup;
- подробные EL3 status сохраняются в `RESULT FAIL`, а DSS exit code
  нормализуется в общий диапазон 0–7 из раздела 6.2;
- production harness обнаружил и позволил исправить затирание FIFO byte count
  в `OPEN_FIFO`; независимые Stage 5 ASM mock-тесты сохранены;
- `EL3LB -n 100` намеренно не входит в обычную host-регрессию: отдельная цель
  `make test-exe-stress` прошла 5200 actual-EXE кадров; это не заменяет
  обязательную ручную MAME-проверку;
- manual MAME и real-card пункты выше остаются открытыми. Процедура:
  `docs/STAGE6_TESTING_RU.md`, шаблон:
  `docs/evidence/STAGE6_TEST_TEMPLATE.md`.
- финальные SHA-256 локальной сборки: IMG
  `939deaf7ebc9e9c5b46fa7220175954d09d79f1703454c3ac46a9ec7792f7f77`,
  EL3LB `1941276a3bf0cebf57c5d8aa8644d4d035eeefb2963200764bfe5f83a94fefaa`,
  EL3TX `d2690e6a7fc42b009da49241b92ff3d25d04b086bb46889153e5e055ee180481`,
  EL3RX `a4b0f975e76c853a34f31248e5edf4a00aecb2e976761e46ccab663308151d14`.

### Этап 7. NETDRV, конфигурация, ARP и DHCP acquire

Результат: независимый link-layer API, транзакционная конфигурация, ARP и
начальное получение DHCP lease. Renewal/RELEASE и `NET_STATE` отложены.

- [x] Реализовать `NETDRV` поверх `EL3`: INIT/DONE, TX/RX/discard, snapshot,
  link state и bounded link wait; caller buffers не используют фиксированный ABI.
- [x] Адаптировать Ethernet II, Internet/UDP checksum, ARP и конфигурационный
  код из BSD RTL-проекта с attribution, без `RTL.*`, старой карты памяти и
  прямого ISA-доступа.
- [x] Реализовать versioned AUTO/explicit slot/base, ID-port и MAC override.
- [x] Реализовать `NETCFG.EXE`, APPINFO-only `NET.CFG`, LF/CRLF/comments,
  warnings для unknown/duplicate и транзакционную публикацию/rollback.
- [x] Реализовать static mode `IFUP.EXE`.
- [x] Реализовать свежий DHCP DISCOVER/OFFER/REQUEST/ACK, NAK/timeout,
  интервалы 4/8/16/16 и атомарную публикацию lease без static fallback.
- [x] Реализовать узкий IPv4/UDP transport для DHCP без публичного Stage 8/9 API.
- [x] Реализовать четыре ARP cache entries, lifetime 60 секунд и oldest eviction.
- [x] Создать developer-only `ARP.EXE`.
- [x] Автоматически проверить ARP request/reply, malformed consume,
  cache hit/expiry/eviction, gateway/broadcast/unknown и DHCP wire/checksums.
- [x] Расширить actual-EXE harness виртуальными файлами/environment/time и
  реактивными ARP/DHCP ACK/NAK/drop сценариями.
- [x] Добавить host responder для macOS BPF/Linux AF_PACKET, classic pcap и
  единый runbook `docs/STAGE7_TESTING_RU.md`.
- [x] Проверить общий `unet.inc` побайтно относительно ESP и RTL sibling ABI.
- [x] Выполнить полный Stage 7 MAME gate перед Stage 8.
- [ ] Выполнить проверки на реальном Sprinter/3C509B-TPO (не блокирует переход,
  но остаётся обязательным открытым evidence).
- [x] Добавить MAME логи/screenshots/pcap:
  `docs/evidence/STAGE7_MAME_2026-08-31.md`.
- [ ] Добавить hardware логи/pcap: ____________________
- [x] Критерий этапа: MAME разрешает MAC соседа/шлюза и получает DHCP lease;
  hardware-критерий остаётся открыт до появления карты.

### Этап 8. IPv4, ICMP и PING

Результат: полноценный `PING.EXE` уровня RTL-кита.

- [x] Реализовать IPv4 header build/parse/checksum.
- [x] Реализовать ICMP Echo Request/Reply.
- [x] Реализовать ICMP timeout и Destination Unreachable.
- [x] Добавить параметры PING `-t`, `-n`, `-l`, `-i`, `-w`.
- [x] Создать тестовый `PINGALT.EXE` с независимым минимальным polling-циклом для
  сравнения при регрессиях основного PING/dispatcher.
- [x] Проверить payload 0/1/32/1472, TTL 1/255 и sequence wrap ASM-векторами и
  actual-EXE harness.
- [x] Проверить неверные IPv4/ICMP checksum, повреждённый payload и несвязанный
  ICMP reply без продления тайм-аута.
- [x] Проверить local peer, gateway и routed external address в actual-EXE
  harness для static/DHCP окружения.
- [x] Выполнить автоматический прогон 1000 запросов без потерь, зависания и
  утечки DSS-страницы.
- [x] Добавить standard-library BPF/AF_PACKET responder, classic pcap,
  `tools/stage8-mame.sh` и runbook `docs/STAGE8_TESTING_RU.md`.
- [x] Добавить IMG `PING/PINGALT/PING.TXT/TESTING.TXT`; в ZIP оставить только
  пользовательские `PING.EXE` и `PING.TXT`.
- [x] Автоматическая приёмка: `make test-host package image` и actual-EXE
  сценарии. SHA-256 чистой сборки 2026-09-01: ZIP
  `5e6cb67d6822e85648bd2d1a5c7fbbe6f95abdce798a2a358661df9382de18f2`, IMG
  `b350fe3c9d19ad9d2a0606748c2e556354d9651721e68032011d9b67c07f2685`,
  PING `7d7a8227f6738404d2191cc69a281709c79976655e4df86e4aa39cab5d9bf965`,
  PINGALT `855a5a32a134e03b35063562e56e5667da1581623e80691b0112338b01bc4d58`.
- [x] Проверка отмены приведена к реальному формату `DSS_SCANKEY`: Ctrl+C
  распознаётся как `B.X_CTRL + D=#AC`, а Esc сохраняет прежнее поведение;
  actual-EXE harness проверяет оба варианта для PING/PINGALT и Stage 7.
- [x] Выполнить MAME gate из `docs/STAGE8_TESTING_RU.md`; успешный gate разрешает
  переход к Stage 9, не закрывая аппаратный долг. Ручной endurance-прогон:
  100/100 запросов; автоматический actual-EXE gate: 1000/1000.
- [ ] Выполнить проверки на реальном Sprinter/3C509B-TPO.
- [x] Добавить MAME evidence `docs/evidence/STAGE8_MAME_2026-09-01.md`.
- [ ] Добавить hardware логи/pcap: ____________________
- [x] Критерий этапа: отсутствие утечек памяти и зависаний при потерях/тайм-аутах.

### Этап 9. UDP и TFTP

Результат: UDP API и передача файлов по TFTP.

- [x] Перенести UDP build/parse/checksum.
- [x] Создать `UDPTEST.EXE` с echo и генератором пакетов.
- [x] Реализовать TFTP GET и PUT.
- [x] Реализовать retry, timeout, duplicate block и unknown transfer ID.
- [x] Реализовать пользовательский host:port.
- [x] Реализовать RFC 2348 `blksize` с безопасным пределом памяти.
- [x] Добавить overwrite/отказ от перезаписи.
- [x] Автоматически проверить пустой, короткий, кратный блоку и файл
  `256 KiB + 123` в обоих направлениях actual-EXE harness.
- [x] Автоматически проверить потерю, повтор и перестановку UDP-пакетов.
- [x] Добавить raw BPF/AF_PACKET responder, отдельный MAME image workflow,
  byte-exact verify и classic-pcap checker без FCS.
- [x] Выполнить чистую автоматическую приёмку `make test-host package image`:
  90 actual-EXE UDP/TFTP сценариев, ASM codecs, writable DSS filesystem,
  responder/pcap vectors, strict IMG/ZIP и отсутствие EXE BSS. SHA-256
  2026-09-01: IMG `f6eaaf4424092d112c28436f6fb3acc09a5a1cfa2494c407fe5f8a7a899ca06c`,
  ZIP `9f428a5cf1e7a2981066f9ab70740ec39016b5e52fdd41412f06cd03f26b2239`,
  UDPTEST `4f3ecc3948e02c9bcefc4e51650f9e8ab1eef89c1634adb62f519817da8a7d4c`,
  TFTP `6051051370321e68ea06e30fc72922a1420ac41b6bb3066877a87bea7de5d9bb`.
- [x] Выполнить единый MAME gate из `docs/STAGE9_TESTING_RU.md` и добавить
  screenshots, responder log и byte-exact pcap:
  `docs/evidence/STAGE9_MAME_2026-09-01.md`.
- [ ] Выполнить проверки на реальном Sprinter/3C509B-TPO.
- [ ] Добавить hardware логи/pcap: ____________________
- [x] Критерий этапа: GET/PUT побайтно сохраняют файл при нормальной сети и потерях.

### Этап 10. DHCP renewal, DNS и NTP

Результат: автоматическая конфигурация сети и базовые сервисы.

- [x] Реализовать DHCP renewal и RELEASE поверх Stage 7 acquire.
- [x] Добавить взаимоисключающие `IFUP -r` и `IFUP -d` с разбором CLI до
  ISA-доступа. Активный lease определяется по `NET_IP_SRC=DHCP`, `NET_IP`,
  `NET_DHCP_SRV` и `NET_LEASE_SEC`; отдельный `NET_STATE` не добавляется.
- [x] Renewal выполняет три DHCPREQUEST с `ciaddr` и тайм-аутом 5000 мс.
  ACK атомарно публикует lease, наследует отсутствующие mask/gateway/DNS и IP
  при нулевом `yiaddr`; NAK очищает динамические поля, а timeout, malformed и
  cancel сохраняют старый lease.
- [x] RELEASE выполняется best-effort по сохранённым данным, затем очищает
  динамические поля; повторный `-d` без lease успешен без передачи. Static mode
  отклоняется, automatic static fallback отсутствует.
- [x] Публиковать при acquire IP, mask, gateway, DNS, server и lease metadata.
- [x] Реализовать backend-neutral DNS A/IN codec и resolver: RD, отдельные
  transaction ID/local UDP port, caller-owned buffers, bounded ASCII labels и
  strict compression pointer/count/type/class/RCODE/A-record parsing.
- [x] Реализовать три попытки по 5000 мс, непродлеваемый deadline для stale,
  foreign и bad-checksum frames, DNS1→DNS2 fallback для временных ошибок и
  финальный NXDOMAIN. Явный DNS-сервер используется без fallback.
- [x] Создать `NSLOOKUP.EXE`; подключить IPv4-literal/hostname resolver к
  `PING`, `UDPTEST`, `TFTP` и `NTP`, сохранив TFTP `host[:port]`.
- [x] Реализовать polling-only NTPv4/SNTP framing, три попытки по 5000 мс и
  проверку endpoint, checksum, version 3/4, mode, LI, stratum, originate cookie
  и transmit timestamp.
- [x] Создать `NTP.EXE`, Gregorian conversion для 1970–2036 и установку часов
  через `DSS_SETTIME` только после закрытия ISA window.
- [x] Добавить общий TZ parser для `NETCFG` и `NTP`: empty/часовая/`H:MM`
  формы, четверть часа, диапазон `-12:00..+14:00`, signed minutes и переходы
  суток/месяцев/лет. Вывод NTP нормализован как `UTC+05:45`.
- [x] Перенести hostname/DNS/NTP/scratch state в DSS-страницу, сохранить
  TX `#4000/#0800`, RX `#4800/#0800`, TFTP file `#5000/#2000` и добавить
  compile-time bounds/stack/EXE assertions без zero-filled BSS.
- [x] Расширить actual-EXE harness DHCP/DNS/NTP сценариями, полным
  `DSS_SETTIME`, ISA/page/stack guards и transactional environment rollback.
- [x] Добавить standard-library `stage10_responder.py`, fault profiles,
  deterministic services, public DNS/NTP UDP proxy, classic pcap checker и
  единый `stage10-mame.sh` workflow.
- [x] Добавить `NSLOOKUP.EXE/TXT` и `NTP.EXE/TXT` в IMG/ZIP, а `S10TEST.TXT`
  только в IMG; runtime-текст проходит CP866/CRLF и strict uppercase 8.3.
- [x] Выполнить автоматическую приёмку: Stage 10 ASM vectors, 54 actual-EXE
  DHCP/DNS/hostname/NTP/TZ/cleanup сценариев, четыре responder/pcap теста,
  проверки ABI, layout, EXE headers/BSS и manifest. Чистый прогон
  `make test-host package image` от 2026-09-02: IMG
  `c2ec897e030b4738e602141a1c17a5be25433fd22975ccae5d7d7e6aa39e23a0`,
  ZIP `c3c22f1e292d16f9a31a54f12360d4aa9c590f098b795e29af7cdb600ec2c8a3`,
  IFUP `af2d2361f046204b62b9e2ae6ce05c45870eb79e90dc2d2b8c2f7463f614e26a`,
  NSLOOKUP `fc6cf52efc3834071cfbdf0e14ad6eb804425d6231f83fae2e694ae28c3768ec`,
  NTP `e3fe0f223b514f1cf665d4e1e5a73e0648ab2a314b690eefaa716f20613d75fa`.
- [ ] Проверить локальные и публичные DHCP/DNS/NTP-сервисы.
- [ ] Выполнить единый MAME gate из `docs/STAGE10_TESTING_RU.md` и добавить
  screenshots, responder log и pcap: ____________________
- [ ] Выполнить проверки на реальном Sprinter/3C509B-TPO.
- [ ] Добавить hardware логи/pcap: ____________________
- [ ] Критерий этапа: `CONNECT.BAT` поднимает сеть без ручного IP и разрешает имя.

### Этап 11. TCP

Результат: клиентский TCP с двумя параллельными каналами.

- [x] Перенести TCP state machine через `NETDRV`.
- [x] Реализовать SYN, ESTABLISHED, FIN и RST paths.
- [x] Реализовать MSS, sequence/acknowledgement и checksum.
- [x] Реализовать retransmit и конечные тайм-ауты.
- [x] Реализовать два одновременно открытых TCP-канала.
- [x] Создать `TCPTEST.EXE`.
- [x] Проверить duplicate и out-of-order segments.
- [x] Проверить zero window, remote FIN/RST и connect timeout.
- [x] Проверить повторное соединение после ошибки.
- [x] Выполнить автоматическую приёмку: executable TCP checksum/MSS/bounds
  vectors, 42 actual-EXE state/multichannel/fault сценариев, три responder/pcap
  теста, layout/EXE/manifest checks. Полный прогон
  `make test-host package image` от 2026-09-03: IMG
  `967827330d1c8686c430827f07d2e3b689dd62063ee85c3ee6b4d8486fefbeb0`,
  ZIP `bd2d276be69d0e6a364a2f11b0166a85c39f3837cf14078a5f5f87b45b5d2fac`,
  TCPTEST `4931eb618266c701747e719e60c7f51671fe409c20c347012b24985fd67ac2d5`.
- [x] Выполнить единый MAME gate из `docs/STAGE11_TESTING_RU.md`: clean/faults/
  zero/reset pcap прошли проверку, screenshots и capture summary сохранены в
  `evidence/stage11-user/`.
- [ ] Выполнить проверки на реальном Sprinter/3C509B-TPO.
- [ ] Добавить hardware логи/pcap: ____________________
- [ ] Критерий этапа: два канала обмениваются данными и корректно закрываются.

### Этап 12. WGET

Результат: загрузчик HTTP-файлов уровня RTL-кита.

- [x] Реализовать HTTP/1.0 GET по обычному TCP.
- [x] Поддержать DNS-имя, IPv4, пользовательский порт и путь.
- [x] Поддержать до пяти redirect.
- [x] Поддержать явное имя выходного файла и overwrite/resume/cancel prompt.
- [x] Поддержать resume через DSS `MOVE_FP`, `Range: bytes=N-` и обязательную
  проверку ответа 206 до первой записи.
- [x] Реализовать запись на диск блоками по 6 КБ без runtime BSS в EXE.
- [x] Показывать sibling-compatible размер, прогресс, время и среднюю скорость;
  `-d` печатает точку на flush и не отключает summary. Прогресс перерисовывается
  на каждом сбросе файлового буфера (каждые 6 КБ): раньше печатался каждый
  четвёртый сброс, то есть раз в 32 КБ, и закачка показывала две-три цифры за
  всё время, что читалось как зависание между ними.
- Итоговая строка `N bytes in T sec, K KB/s` меряет весь ход, включая DNS,
  ARP, connect, redirect-хопы, `CLOSE` (до 5 с) и закрытие файла, а T — целое
  число секунд с RTC. На короткой закачке это не скорость передачи, а скорость
  всей операции; честное измерение — задача `DLSPEED.EXE` (перенесена в
  Этап 13).
- [x] Обработать Content-Length и body-until-close.
- [x] Обработать 200, 206, 3xx, 4xx/5xx, timeout, отмену и преждевременный
  разрыв; error body не записывается, partial-файл сохраняется для resume.
- [x] Зафиксировать golden console output относительно
  `sprinter-rtl8019a@9ec98b0`; нормализуются только баннер и аппаратная строка
  `REGS`.
- [x] Добавить actual-EXE HTTP harness с DSS `MOVE_FP`, deterministic responder,
  static/ASM checks, MAME workflow и IMG/ZIP artifacts.
- [x] Выполнить локальную автоматическую приёмку: 53 actual-EXE CLI/golden/
  HTTP/file/resume/fault сценария, четыре responder unit tests, static/ASM,
  manifest и FAT12 extraction checks. Прогон `make test-host package image` от
  2026-09-03: IMG `96830030e236979eb4f23dd3dddd601dca27907d204e381f87142773e88ffd1e`,
  ZIP `abda20485d25f14af207c09a4ff5c2d511e83607f0f8278f629d436dc800afc2`,
  WGET `dc0866f2f8fdea687970e11c8aea66309ee051808aa9b8c1a726b69f99e38593`.
- [ ] Проверить нулевой, малый и большой файл в MAME и на карте.
- [ ] Выполнить единый ручной MAME gate из `docs/STAGE12_TESTING_RU.md`,
  сохранить screenshots, responder log, pcap и evidence summary.
- [ ] Выполнить проверки на реальном Sprinter/3C509B-TPO.
- [ ] Добавить MAME/hardware результаты: ____________________
- [ ] Критерий этапа: полная и продолженная загрузка совпадает по checksum.

### Этап 13. FTP

Результат: CLI-клиент FTP уровня сиблинга (`sprinter-rtl8019a/ftp.asm`,
BSD-3-Clause, дизайн-переиспользование разрешено), а не интерактивная сессия с
построчным вводом. Решения зафиксированы 2026-09-05 (см. также
`docs/FTP.md`):

1. **One-shot CLI**, не командный цикл: `FTP host[:port] path [флаги]`,
   `FTP host[:port] PUT local [флаги]`, `FTP host[:port] [path] -l|-n [флаги]`,
   `FTP /?`.
2. **Без PWD/CWD** — удалённый путь передаётся аргументом RETR/STOR/LIST, как
   у сиблинга; результат этапа — CLI-клиент, а не интерактивная FTP-сессия.
3. **`DLSPEED.EXE` перенесён из Этапа 16 в этот этап** — честное измерение
   скорости готово одновременно с FTP, для совместной приёмки.

В отличие от сиблинга, control и data — два независимых нативных TCP-канала
(`TCPX` канал 0/1), без `SAVE_CTX`/`RESTORE_CTX`: `226` в pending канала 0,
пока ещё качается канал 1.

- [x] Реализовать USER/PASS и обработку кодов ответа (`READ_REPLY`:
  многострочный ответ, финальная строка — 3 цифры + пробел).
- [x] Реализовать passive mode PASV (`PARSE_PASV`, порт из `227`).
- [x] Реализовать TYPE I, SIZE (не фатален на 5xx), LIST (терминал вместо
  файла). PWD/CWD не реализованы (см. решение выше); **NLST не реализован
  отдельно от LIST** — `-n` посылает тот же `LIST`, что и `-l` (см. "Известные
  ограничения" ниже).
- [x] Реализовать RETR и STOR. STOR отправляет данные через `TCPX.SEND`,
  которая сама сегментирует и подтверждает каждый MSS — ограничение см. ниже.
- [x] Реализовать REST resume (offset из `FILE.OPEN_OUTPUT_ORC`, отказ сервера
  на REST — фатален, без порчи partial-файла).
- [x] Использовать отдельные control и data TCP-каналы (`STAGE13_LAYOUT` в
  `memory.inc`: два полных pending-региона и контекста по 5×MSS каждый).
- [x] Добавить actual-EXE FTP harness (`tools/exe-harness/harness.js`,
  `ftp`-режим: push `220`, разбор команд, self-announced data-порт,
  fault-инъекции), host responder (`tools/host/stage13_responder.py`) и его
  unit-тесты, и `tools/test-stage13-exe.js` (47 сценариев: FTP + DLSPEED,
  golden-транскрипты `tools/test-fixtures/stage13-ftp-golden.json` с пином
  ревизии сиблинга).
- [ ] Проверить anonymous и password login в MAME.
- [ ] Проверить пустой каталог, большой список и имена в пределах DSS в MAME.
- [ ] Проверить upload/download/resume и отказ сервера в MAME.
- [ ] Проверить MAME и реальную карту.
- [ ] Добавить результаты: ____________________
- [x] Критерий этапа (локально): LIST, download, upload и resume проходят
  побайтную проверку через actual-EXE harness, включая файлы, требующие
  повторного заполнения глубокого receive window на канале данных (бывший
  дефект на 3216 байт, см. раздел с найденными и исправленными дефектами
  ниже — исправлен и подтверждён побайтной загрузкой 20000 байт). MAME/
  реальная карта остаются открытыми пунктами.

**Известные ограничения (зафиксированы намеренно, не переоткрывать как
баги):**

- **Нет NLST-fallback.** У сиблинга `-n` шлёт `NLST` и переходит на `LIST` при
  5xx. В этой сборке `-n` всегда шлёт `LIST` — отдельная команда `NLST` и
  retry-логика не влезли в потолок образа 16256 байт вместе со всем остальным,
  что нужно PASV FTP (см. `docs/FTP.md`).
- **`STOR` работает stop-and-wait.** `TCPX.SEND` держит в полёте не больше
  одного MSS за вызов независимо от окна получателя, поэтому `PUT` не
  получает того же выигрыша от глубокого receive window, что `RETR`. Это
  известное, задокументированное ограничение транспорта, а не дефект этого
  этапа.
- **Строки UX короче, чем у сиблинга.** План требовал взять их дословно;
  дословные не поместились в тот же потолок 16256 (`Host X -> Y` вместо
  `Resolved X -> Y`, `Opening data...`, `Done. N bytes recv.` и т. д.).
  Последовательность диалога и коды выхода совпадают с сиблингом, отличается
  только формулировка; зафиксировано в `tools/test-fixtures/
  stage13-ftp-golden.json` вместе с пином ревизии сиблинга.
- **Условная сборка транспорта разведена одним флагом, а не двумя.** План
  предлагал `TCPX_DEEP_WINDOW` + `APP_STANDARD_LAYOUT`; реализован
  производный `TCPX_SINGLE_CONTEXT EQU STAGE12_LAYOUT AND NOT
  STAGE13_LAYOUT` (три места в `tcp_transport.asm`). Функционально это то же
  самое — глубокое окно и штатная раскладка остаются на `STAGE12_LAYOUT`,
  который FTP тоже определяет, а от одноканальности FTP отписывается, — но
  дешевле: полный переименовывающий рефакторинг задел бы каждый
  `IFDEF STAGE12_LAYOUT` в транспорте ради гейта «WGET байт-в-байт» и нулевой
  функциональной разницы. Цена — имя `STAGE12_LAYOUT` теперь означает
  «штатная раскладка + глубокое окно», а не «WGET».

**Дефекты, найденные и исправленные при тестировании этого этапа** (оставлены
здесь как честная запись, а не скрыты постфактум; не путать с разделом выше —
это настоящие баги, а не согласованные компромиссы; ни один сейчас не
открыт):

- ~~GET останавливался ровно на 3216 байт при повторном заполнении глубокого
  receive window на канале данных (TCP context 1)~~ — **исправлено
  2026-09-05.** Корень: `HANDLE_SEGMENT` (`tcp_transport.asm`) писал длину
  входящего сегмента в `S11_COPY_LENGTH` — ту же ячейку, где `RECV` хранит
  вместимость буфера вызывающего кода на время ожидания. Поскольку
  `HANDLE_SEGMENT` вызывается изнутри `WAIT_FOR_EVENT`, которую сама `RECV`
  и вызывает, при повторном входе значение подменялось. FTP — первое
  приложение, запрашивающее у `RECV` меньше одного MSS за вызов (буфер
  `S11_APP_BUFFER` — 448 байт против MSS 536), поэтому `RECV_COPY` доверял
  подменённой длине и копировал целый сегмент в буфер меньшего размера,
  затирая `RUNTIME_BASE` (в частности `EL3_BASE`/`EL3_IDPORT`, идущие сразу
  за `S11_APP_BUFFER` без зазора в `STAGE13_LAYOUT`) — отсюда и
  низкоуровневая ошибка EL3-регистра вместо обычной TCP-ошибки. Исправление:
  `HANDLE_SEGMENT` использует отдельную ячейку `S11_SEGMENT_LENGTH` вместо
  `S11_COPY_LENGTH` (7 замен, без изменения раскладки памяти). Подтверждено
  побайтной загрузкой 5000 и 20000 байт с `maxInFlight=2680`;
  `tools/test-stage13-exe.js`'s `BEYOND_WINDOW` теперь проверяет успешную
  загрузку вместо фиксации бага; Stage 11/12 регрессия не задета.
- ~~RST на канале данных вне активного `WAIT_FOR_EVENT` мог отрапортовать
  `RESULT OK` с усечённым файлом~~ — **исправлено 2026-09-05.**
  `TCP_STATE_CLOSED` равен `0` (`tcp.inc`), и `TCPX.RECV`'s pre-check
  (`.RECV_STATE_ERROR`, до входа в ожидание) читал `CTX_STATE==0` как
  обычное закрытие (`TCP_ERR_CLOSED`), не проверяя `CTX_LAST_STATUS`, куда
  `ACCEPT_RST` уже записывает `TCP_ERR_RESET`. Живой `WAIT_FOR_EVENT`-путь
  RST всегда обрабатывал правильно (`BIT 3,A` → `TCP_ERR_RESET`); ломалась
  только "отложенная" проверка, когда RST на data-канале обработан фоново
  (например, во время ожидания ответа control-канала на `REST`) до того, как
  приложение вызвало `RECV` снова. Исправление: `.RECV_STATE_ERROR` теперь
  возвращает `CTX_LAST_STATUS`, если он не нулевой (ноль — единственное
  значение, которое `CLEAR_CONTEXT`/`RESET` дают при обычном закрытии или
  ещё не открытом канале; ни один реальный `TCP_ERR_*`/`NETDRV_ERR_*` не
  равен нулю), и только иначе — прежний безусловный `TCP_ERR_CLOSED`.
  Точечное изменение в `tcp_transport.asm`, без изменения `.RECV_CLOSED`
  (FIN остаётся безусловным `TCP_ERR_CLOSED`, как и раньше). Подтверждено:
  Stage 11/12/13 регрессия (42+53+34 сценария) не задета; отдельный
  целевой regression-тест на саму гонку не добавлен — риск неверно
  смоделировать её в harness показался выше пользы для этой правки, но
  логика читается напрямую по коду `.RECV_STATE_ERROR`.
- ~~Значение флага затирало ещё не разобранный хвост той же командной
  строки~~ — **исправлено 2026-09-06 (код-ревью).** `F13_USER`/`F13_PASS`/
  `F13_OUTPUT_OVERRIDE` намеренно занимают диапазон страничной копии
  командной записи (`S10_PAGE_COMMAND_BUFFER`, `PAGE_BASE+0x3E00`): ниже
  `0x3E00` места не осталось. Но `CMDLINE_SOURCE` указывал именно на эту
  копию, поэтому `COPY_TOKEN` для `-p`/`-o` писал значение поверх текста,
  до которого читатель ещё не дошёл. `FTP host -p secret FILE.BIN` уходил на
  сервер как `RETR secret`. Исправление: под `STAGE13_LAYOUT`
  `ALLOCATE_FRESH` не делает страничную копию вовсе и оставляет `IX` на
  WIN1-копии `S10_COMMAND_BUFFER` (`0x4000`), которую парсер ничем не
  затирает; зануление страницы расширено до конца F13-блока. Добавлен
  `ASSERT F13_USER == S10_PAGE_COMMAND_BUFFER` (перекрытие теперь
  задокументировано и зафиксировано), гейт в `check-stage13.pl` и два
  сценария с флагами перед позиционным аргументом. WGET не задет
  (байт-в-байт 16050).
- ~~`226 Transfer complete` терялся на коротких передачах~~ — **исправлено
  2026-09-06 (код-ревью).** `READ_REPLY` обнулял аккумулятор на входе, а
  сервер вправе прислать `150 ...\r\n226 ...\r\n` одним сегментом (для
  мелкого файла это обычное дело). Разобрав `150`, FTP выбрасывал уже
  пришедший `226`, не печатал его и затем впустую выжидал
  `FTP_REPLY_TIMEOUT_MS`. То есть ровно то преимущество двух каналов, ради
  которого этап и делался, на практике не работало для коротких файлов.
  Аккумулятор — потоковый буфер, `CONSUME_LINE` его и так корректно
  сдвигает, а `CLEAR_FTP_STATE` обнуляет один раз на старте; сброс просто
  убран. Короткая загрузка в harness: 2 684 400 → 214 772 шага и `226` в
  выводе. Golden-транскрипты `get`/`list` теперь содержат `226`.
- ~~Ответ сервера длиннее 260 байт писал мимо `F13_REPLY_LINE`~~ —
  **исправлено 2026-09-06 (код-ревью).** Аккумулятор — 512 байт, а
  `F13_REPLY_LINE` — 256; копирование строки не ограничивалось, и длинный
  баннер `220` (реальные серверы такие шлют) уходил в `F13_SAVED_CWD` —
  путь, который FTP восстанавливает при выходе. Добавлено усечение до 255
  байт и сценарий с 400-символьным баннером.
- ~~Канал данных бросался открытым при `550` на RETR/STOR/LIST и при отказе
  в `REST`~~ — **исправлено 2026-09-06 (код-ревью).** Оба пути приходят в
  `REMOTE_FAIL_COMMON` уже с установленным PASV-соединением, а закрывался
  только control-канал: сервер оставался с полуоткрытой сессией до
  собственного таймаута. Теперь перед закрытием control-канала выдаётся
  `TCPX.ABORT` на канал данных (для `PASV_FAIL`, где канала ещё нет, это
  no-op). Harness получил учёт RST по портам (`tcpResetPorts`), проверка
  добавлена в оба сценария.

**Дефекты тестовой инфраструктуры, найденные при живой приёмке в MAME**
(в `tools/host/stage13_responder.py`, не в прошивке; вскрылись только на
реальном прогоне — юнит-тесты гоняли `Responder.handle()` заготовленными TCP-
кадрами, минуя ARP/DNS/REST-семантику целиком):

- ~~`stage13_responder.py` не отвечал на ARP~~ — **исправлено 2026-09-07.**
  `Responder.handle()` уходил прямо в `stage11.parse_tcp`, и ARP-запрос
  `who has 192.168.7.44` не получал ответа — ни один TCP/UDP пакет физически
  не мог уйти. Добавлена та же ветка, что уже была в `stage11_responder.py`/
  `stage12_responder.py`.
- ~~`stage13_responder.py` не отвечал на DNS~~ — **исправлено 2026-09-07.**
  Порт 53 не был обработан вовсе; `NSLOOKUP`/`FTP ftp.stage13.test` выжидали
  полный таймаут резолвера (DNS1→DNS2, до ~30 c) и падали с `EL3_ERR_RX_TIMEOUT`
  (код 14). Добавлена DNS-ветка по образцу `stage12_responder.py`
  (`DNS_NAME = "ftp.stage13.test"`, как в `docs/STAGE13_TESTING_RU.md`).
- ~~`REST` принимался (`350 Restarting at N`), но полностью игнорировался~~ —
  **исправлено 2026-09-08.** `RETR` всегда пересылал фикстуру с байта 0
  независимо от предшествующего REST; `FTP LARGE.BIN -r` против уже
  докачанного файла заново получал все 3000 байт и дописывал их в конец,
  задваивая содержимое вместо честного докачивания хвоста. Исправление:
  offset от `REST` запоминается и вычитается ровно из одного следующего
  `RETR` (как у настоящего сервера — не переживает второй `RETR` без своего
  `REST`).
- Дополнительно: `stage13_responder.py` научился отвечать на обычный HTTP GET
  (порт 80, без Range/redirect — ровно то, что требует DLSPEED), чтобы
  совместный замер скорости не требовал второго responder'а для этого же
  образа. Первая версия отдавала FTP-шный `LARGE.BIN` (3000 байт) — живой
  прогон показал, что этого мало: на виртуальном линке передача укладывается
  в одну RTC-секунду, и DLSPEED либо отказывает с "sample too short", либо
  даёт грубую оценку. Заведена отдельная 512-КиБ фикстура `HTTP_LARGE`,
  не связанная с FTP-шным `LARGE.BIN` (у того своя роль — контролируемое
  заполнение 5-MSS окна, трогать размер нельзя).
- Логирование: каждая control-команда пишется в лог как `CMD '...'`
  (repr, значит виден посторонний байт/регистр/обрезание) — добавлено как
  диагностика при расследовании этих дефектов, полезно оставить постоянно.

### Этап 14. UNET DLL и bindings

Результат: совместимый программный интерфейс для сторонних приложений.

- [ ] Перенести `unet.inc` без изменения ABI v1.0.
- [ ] Добавить build-time проверку идентичности общего ABI.
- [ ] Собрать `UNET509B.DLL` в формате libman L1.
- [ ] Заявить capabilities TCP, UDP, RESOLVE, PING и MULTICHAN.
- [ ] Не заявлять LISTEN, RAWETH, RXFLOW и ASYNCSEND.
- [ ] Реализовать два TCP/UDP-канала.
- [ ] В GETINFO вернуть backend `509B` и строку `slot/#base`.
- [ ] Реализовать диагностический LASTERR.
- [ ] `RXPAUSE`/`RXRESUME` оформить безопасным no-op success.
- [ ] Неподдерживаемые SETOPT возвращают `NERR_NOTSUP`.
- [ ] Создать `UNETTEST.EXE`.
- [ ] Добавить backend-neutral C и Pascal bindings и примеры.
- [ ] Проверить DLL в MAME и на реальном Sprinter.
- [ ] Добавить результаты: ____________________
- [ ] Критерий этапа: существующие UNET-примеры работают без backend-specific кода.

### Этап 15. TELNET и передачи файлов

Результат: интерактивный терминал с возможностями соседнего кита.

- [ ] Реализовать Telnet negotiation без бесконечных negotiation loops.
- [ ] Реализовать ANSI/VT100 terminal handling.
- [ ] Реализовать локальный escape для выхода и настроек.
- [ ] Перенести YMODEM/ZMODEM на общий TCP/file API.
- [ ] Проверить ввод, вывод, управляющие последовательности и изменение экрана.
- [ ] Проверить отмену передачи и потерю соединения.
- [ ] Проверить MAME и реальную карту.
- [ ] Добавить результаты: ____________________
- [ ] Критерий этапа: длительная интерактивная сессия и передача файла стабильны.

### Этап 16. Стабильность, документация и релиз

Результат: воспроизводимые IMG и ZIP, готовые к тестированию и распространению.

- [x] `DLSPEED.EXE` перенесён и реализован в Этапе 13 — см. соответствующий
  раздел; здесь остаётся зафиксировать скорость на MAME/реальной карте.
- [ ] Создать `DLDIRECT.EXE` и `DLDIRCP.EXE` для сравнения прямого сетевого пути
  без диска с общей TCP/download реализацией.
- [ ] Выполнить `make test-host` для LFSR, EEPROM/base encoding, checksums,
  parsers, TCP state transitions и manifest validation.
- [ ] Выполнить 1000 PING без зависания.
- [ ] Выполнить многократные TFTP GET/PUT.
- [ ] Загрузить большой файл через WGET и FTP.
- [ ] Выполнить длительную TELNET-сессию.
- [ ] Проверить link loss/up во время каждого протокола.
- [ ] Проверить reset карты и повторный INIT.
- [ ] Проверить недоступный сервер, DNS failure и заполнение диска.
- [ ] Проверить все polling loops на конечный тайм-аут.
- [ ] Подготовить README, быстрый старт, руководство по конфигурации и UNET.
- [ ] Подготовить описание диагностики PnP/classic activation.
- [ ] Сформировать IMG и ZIP через единый manifest.
- [ ] Проверить содержимое и форматы артефактов.
- [ ] Записать версии инструментов и SHA-256 итоговых файлов.
- [ ] Добавить release test report: ____________________
- [ ] Критерий этапа: вся матрица приёмки пройдена, артефакты воспроизводимы.

## 9. Протокольные требования

- ARP: [RFC 826](https://www.rfc-editor.org/rfc/rfc826).
- IPv4: [RFC 791](https://www.rfc-editor.org/rfc/rfc791).
- ICMP: [RFC 792](https://www.rfc-editor.org/rfc/rfc792).
- UDP: [RFC 768](https://www.rfc-editor.org/rfc/rfc768).
- DHCP: [RFC 2131](https://www.rfc-editor.org/rfc/rfc2131).
- DNS: [RFC 1035](https://www.rfc-editor.org/rfc/rfc1035).
- TFTP: [RFC 1350](https://www.rfc-editor.org/rfc/rfc1350).
- TFTP block size: [RFC 2348](https://www.rfc-editor.org/rfc/rfc2348).
- NTP: [RFC 5905](https://www.rfc-editor.org/rfc/rfc5905).
- TCP: [RFC 9293](https://www.rfc-editor.org/rfc/rfc9293).
- HTTP/1.0: [RFC 1945](https://www.rfc-editor.org/rfc/rfc1945).
- HTTP Range: [RFC 9110](https://www.rfc-editor.org/rfc/rfc9110).
- FTP: [RFC 959](https://www.rfc-editor.org/rfc/rfc959).
- Telnet: [RFC 854](https://www.rfc-editor.org/rfc/rfc854).

Реализуется только необходимое клиентское подмножество. Любое осознанное
отклонение документируется рядом с соответствующим модулем и проверяется тестом.

## 10. Состав артефактов

### 10.1. Тестовый IMG

Файл: `distr/sprinter-3c509b.img`.

- Формат FAT12, совместимый с образом соседних проектов.
- Плоское содержимое с именами 8.3.
- Содержит пользовательские программы, DLL, конфигурацию, документацию и все
  тестовые/диагностические программы.

Обязательные тестовые файлы:

```text
HELLO.EXE
ISAPROBE.EXE
EL3EEP.EXE
EL3REG.EXE
EL3LB.EXE
EL3TX.EXE
EL3RX.EXE
ARP.EXE
PINGALT.EXE
UDPTEST.EXE
TCPTEST.EXE
UNETTEST.EXE
DLSPEED.EXE
DLDIRECT.EXE
DLDIRCP.EXE
TESTING.TXT
S9TEST.TXT
S10TEST.TXT
S11TEST.TXT
S12TEST.TXT
S13TEST.TXT
```

### 10.2. Пользовательский ZIP

Файл: `distr/sprinter-3c509b.zip`.

Обязательные программы:

```text
EL3INFO.EXE
NETCFG.EXE
IFUP.EXE
PING.EXE
NSLOOKUP.EXE
NTP.EXE
TFTP.EXE
WGET.EXE
FTP.EXE
TELNET.EXE
UNET509B.DLL
```

Обязательные сопутствующие файлы:

```text
CONNECT.BAT
NETSMPL.CFG
README.TXT
READMERU.TXT
HOWTO.TXT
USAGE.TXT
EL3INFO.TXT
NETCFG.TXT
IFUP.TXT
PING.TXT
NSLOOKUP.TXT
NTP.TXT
TFTP.TXT
WGET.TXT
FTP.TXT
TELNET.TXT
UNET509B.TXT
CHANGES.TXT
LICENSE.TXT
```

В ZIP запрещены `HELLO`, `ISAPROBE`, `EL3EEP`, `EL3REG`, `EL3LB`, `EL3TX`,
`EL3RX`, `ARP`, `PINGALT`, `UDPTEST`, `TCPTEST`, `UNETTEST`, `DLSPEED`,
`DLDIRECT`, `DLDIRCP`, host-side scripts, object files, листы ассемблера и
отладочные дампы.

### 10.3. Формат файлов

- Имена внутри IMG/ZIP соответствуют 8.3 и уникальны без учёта регистра.
- Текстовые файлы преобразуются в CP866 с CRLF.
- EXE, DLL и другие бинарные файлы копируются без преобразования.
- Исходные `docs/*.md` хранятся в UTF-8; упаковщик создаёт перечисленные `.TXT`
  и не меняет исходные Markdown-файлы.
- C/Pascal bindings и исходные примеры остаются в исходном репозитории, но не
  включаются в runtime ZIP.
- `docs/MAME_NETWORK.md`, таблица аудита референсов и host-test инструкции
  являются developer-only и в пользовательский ZIP не включаются.
- Для каждой пользовательской EXE существует отдельная короткая Markdown-страница
  с синтаксисом, exit codes, примером успеха и типичными ошибками; `USAGE.md`
  служит общим индексом.

## 11. Общая матрица тестирования

Для воспроизводимости в `tools/host/` создаются серверы/скрипты для Ethernet
frame injection, UDP echo, DHCP, DNS, TFTP, HTTP с Range/redirect/error modes,
FTP, TCP fault injection, Telnet и throughput. Адреса стенда задаются параметрами
и не зашиваются в Z80-исходники. Один и тот же набор payload/golden hashes
используется в MAME и на физической сети.

Каждый evidence bundle именуется `stageNN-YYYYMMDD-{mame|hw}` и содержит:

- команду сборки и запуска;
- версии EXE, MAME/DSS и host tool;
- текстовый лог `PASS`/`FAIL`;
- pcap для сетевого этапа;
- SHA-256 переданного файла, если проверяется передача;
- краткое описание отличий MAME и оборудования.

### 11.1. Низкий уровень

- Все допустимые I/O-базы `0x200..0x3E0` в MAME.
- Реальная EEPROM-база и минимум одна runtime-база на оборудовании.
- Правильный и неправильный ID-порт.
- Карта отсутствует, неверный product ID, PnP wait state.
- Low/high word access без промежуточного I/O.
- FIFO-длины 60, 61, 62, 63 и 1514.
- Два и десять RX-пакетов подряд.
- TX FIFO full, RX empty, link down, CIP timeout.
- TX underrun/jabber и RX error recovery.

### 11.2. Сеть

- Один сегмент без шлюза.
- Сеть со шлюзом и DNS.
- Static IPv4 и DHCP.
- Packet loss, duplication, reordering и задержка.
- Сервер недоступен и соединение разорвано.
- Пустой, минимальный и большой файл.
- Диск заполнен и существующий файл нельзя перезаписывать.
- Два параллельных TCP-канала.

### 11.3. Среды

Каждый этап, создающий исполняемый сетевой код, проверяется:

1. В MAME с 3C509B и pcap backend.
2. На реальном Sprinter с реальной 3C509B-TPO.
3. По возможности одним и тем же host-side сценарием и эталонным payload.

Лог должен содержать версию программы, I/O-базу, MAC, параметры теста, итоговые
счётчики и явный `PASS`/`FAIL`.

### 11.4. Автоматические host-side регрессии

`make test-host` обязан проверять без запуска полного DSS-сеанса:

- 255 выходных байтов LFSR и повторяемость последовательности;
- encode/decode всех 31 допустимых I/O-баз;
- EEPROM word parsing и порядок байтов MAC на golden vectors;
- 16-битный ISA8 access trace: low byte строго перед high byte;
- расчёт effective TX length и DWORD padding для 14, 42, 59, 60, 61, 62, 63 и
  1514 байт;
- Internet/UDP/TCP/ICMP checksums, включая нечётную длину;
- разбор Ethernet, ARP, IPv4, DHCP, DNS compression, TFTP, HTTP и FTP replies;
- TCP state transitions, retransmit schedule и два channel contexts;
- command-line parser, числовые границы и 8.3 output-name generation;
- artifact manifest, запрет тестов в ZIP и обязательный набор IMG;
- CP866/CRLF только для текстов и побайтную неизменность EXE/DLL.

Если доступен Z80 runner (`z88dk-ticks` или эквивалент), тестируются собранные
ASM-функции, а не их повторная реализация на host-языке. Отдельная копия
алгоритма допустима только как генератор golden vector.

Для этапов 5/6 дополнительно используется `tools/exe-harness/`: неизменённый
MIT Z80 core Molly Howell исполняет настоящий 128-байтовый DSS EXE, загруженный
по адресам его header. Модель предоставляет обычную/страничную память,
GETMEM/SETWIN1/FREEMEM/console/EXIT, оба ISA slot, полный 16-битный port space,
ID sequence, read-only EEPROM, activation и минимальный register/FIFO datapath
3C509B. Неизвестный DSS call, EEPROM write/erase, неверный FIFO offset, DSS при
открытом ISA и выход с открытым ISA/неосвобождённой страницей являются ошибкой
harness. Результат сценария содержит exit code, console, TX frames и cleanup.

Host network helper `tools/host/ethernet_helper.py` использует BPF на macOS и
AF_PACKET на Linux для raw send и строго проверяет classic pcap: MAC, EtherType,
input/wire length, pattern, zero padding, burst count/order и отсутствие FCS.
MAME launcher принимает `MAME_NETWORK_INTERFACE`, сверяет имя с
`mame -listnetwork` и не содержит зашитого `feth0`; текущий стенд использует
MAME=`feth0`, helper/tcpdump=`feth1`.

## 12. Проверка сборки и выпуска

Перед закрытием каждого этапа:

```sh
make build
make test-host
make image
make package
```

Перед релизом дополнительно проверяются:

```sh
unzip -l distr/sprinter-3c509b.zip
mdir -i distr/sprinter-3c509b.img ::
```

Автоматическая проверка manifest должна отклонять:

- отсутствующий обязательный файл;
- тестовый файл в ZIP;
- коллизию 8.3;
- текст не в CP866/CRLF;
- неожиданный build artifact;
- различие между объявленным и фактическим составом IMG/ZIP.

## 13. Definition of Done проекта

Проект версии 1 считается завершённым, когда:

- [ ] Все этапы 0–16 закрыты последовательно.
- [ ] Таблица паритета из раздела 1.4 закрыта без необоснованных пропусков.
- [ ] Все пользовательские программы собраны и документированы.
- [ ] `make test-host` проходит на чистом checkout.
- [ ] Все сетевые программы прошли тесты в MAME и на реальной карте.
- [ ] MAME воспроизводит подтверждённое аппаратное ISA8/FIFO-поведение.
- [ ] IRQ-линии Sprinter остались неподключёнными.
- [ ] В проекте нет кода записи EEPROM.
- [ ] В BSD-исходниках нет скопированного GPL-кода Nestor/Linux.
- [ ] IMG содержит полный тестовый комплект.
- [ ] ZIP не содержит тестовых программ и промежуточных файлов.
- [ ] Имена внутри артефактов соответствуют 8.3.
- [ ] Документация позволяет настроить карту и сеть с чистой DSS-системы.
- [ ] Контрольные суммы опубликованных IMG и ZIP записаны в release notes.
