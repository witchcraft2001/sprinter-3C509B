# Stage 11 MAME evidence — 2026-09-03

The user-run MAME session used the static client `192.168.7.20`, responder
`192.168.7.44:7777`, ISA slot 1 and base `0x0300`.

## Observed results

- `TCPTEST -n 2 -l 0`: two channels, both cycles, `RESULT OK`.
- `TCPTEST -n 2 -l 1`: two channels, both cycles, `RESULT OK`.
- `TCPTEST -n 2 -l 536`: two channels, both cycles, `RESULT OK`.
- `TCPTEST -n 2 -l 537`: two channels, both cycles, `RESULT OK`.
- `TCPTEST -n 2 -l 2048`: two channels, both cycles, `RESULT OK`.
- `TCPTEST -n 2 -l 4096`: two channels, both cycles, `RESULT OK`.
- `TCPTEST -n 1 -l 537` with reset profile: first run `RESULT OK` after one
  injected RST and reconnect.
- Cancellation run: `RESULT FAIL code=23`, confirming Ctrl+C cancellation.

## Captures

The four pcap files in this directory pass the Stage 11 pcap gate:

| Profile | TCP frames | Channels | Data bytes | Result |
| --- | ---: | ---: | ---: | --- |
| clean | 600 | 24 | 28872 | PASS |
| faults | 198 | 4 | 8728 | PASS |
| zero | 116 | 4 | 2148 | PASS |
| reset | 76 | 4 | 1610 | PASS |

Screenshots are stored alongside this report with timestamped names. The
acceptance-significant responder transcript is stored in
`STAGE11_RESPONDER_LOG.txt`; automated pcap validation and representative frame
tails are stored in `STAGE11_CAPTURE_LOG.txt`.
